# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 6. 오후 3:36:47 |
| Language | oraclesql |
| Runtime | 285 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/exchange-seats/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 seat 테이블의 모든 행을 스캔하므로 시간 복잡도는 데이터의 크기(n)에 비례합니다. MAX(id) 값을 얻기 위해 서브쿼리를 사용하지만, 이는 상수 시간에 수행되므로 전체 시간 복잡도에 영향을 미치지 않습니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 원본 테이블의 행 수와 동일하므로 공간 복잡도 또한 데이터의 크기(n)에 비례합니다.

3. **풀이 접근법**: 이 쿼리는 간단한 CASE 문과 서브쿼리를 사용하여 문제를 해결합니다. 짝수와 홀수 행을 구분하여 교환하는 로직을 구현하고, 마지막 행은 교환하지 않는다는 조건을满足합니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. 문제의 요구 사항을 명확하게 이해하고, 적절한 SQL 구문을 사용하여 해결했습니다. 특히, MOD 함수를 사용하여 홀수와 짝수를 구분하는 부분이 효율적입니다.

5. **개선 사항**: 현재 쿼리는 이미 최적화된 상태로 보입니다. 그러나 대안적인 접근법으로는, 서브쿼리를 JOIN 또는 WINDOW 함수와 함께 사용하여 MAX(id) 값을 얻을 수 있습니다. 하지만, 이러한 접근법은 쿼리의 읽기 어려움을 증가시키고, 성능에 큰 영향을 미치지 않을 수 있습니다. 따라서, 현재의 구현은 충분히 효율적입니다.
