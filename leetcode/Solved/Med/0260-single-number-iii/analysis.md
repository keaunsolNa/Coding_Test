# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 4. 오후 3:57:34 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.8 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/single-number-iii/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n^2) - contains() 메서드는 리스트의 모든 요소를 순회하여 검색하므로 O(n)의 시간 복잡도를 가지며, 이 메서드가 리스트의 각 요소에 대해 호출되므로 전체 시간 복잡도는 O(n^2)가 됩니다.

2. **공간 복잡도**: O(n) - 리스트에 최대 n개의 요소를 저장할 수 있으므로 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 코드는 단순한 리스트를 이용한 접근법을 사용합니다. 리스트에 이미 존재하는 요소가면 제거하고, 존재하지 않으면 추가하는 방식으로 두 개의 싱글 번버를 찾습니다. 그러나 이 접근법은 비효율적이며, 특히 큰 입력에 대해 성능이 저하됩니다.

4. **잘된 점**: 코드는 간단하고 이해하기 쉽습니다. 또한 리스트의 contains() 메서드를 사용하여 중복된 요소를 제거하는 방식은 직관적입니다.

5. **개선 사항**: 이 문제는 비트 조작을 사용하여 더 효율적으로 해결할 수 있습니다. XOR 연산을 사용하여 두 개의 싱글 번버를 찾을 수 있습니다. 먼저 모든 요소를 XOR 연산하여 두 개의 싱글 번버의 XOR 결과를 얻습니다. затем, 이 결과의 가장 오른쪽에 있는 1의 위치를 찾습니다. 이 위치를 사용하여 두 개의 싱글 번버를 구분할 수 있습니다. 이 접근법의 시간 복잡도는 O(n)이며, 공간 복잡도는 O(1)입니다. 또한, 해시 맵을 사용하여 각 요소의 빈도수를 계산하는 방식도 있습니다. 이 경우 시간 복잡도는 O(n)이며, 공간 복잡도는 O(n)입니다.
