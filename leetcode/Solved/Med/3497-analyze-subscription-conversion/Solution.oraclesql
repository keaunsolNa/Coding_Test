/* Write your PL/SQL query statement below */
SELECT user_id
     , ROUND(AVG(NVL((CASE WHEN activity_type = 'free_trial' THEN activity_duration ELSE 0 END), 0)), 2) trial_avg_duration 
  FROM UserActivity 
 WHERE activity_type IN('paid', 'free_trial')
 GROUP BY user_id  
 ORDER BY user_id