# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 13. 오전 10:32:34 |
| Language | oraclesql |
| Runtime | 405 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/analyze-subscription-conversion/submissions/)

## Code Review

코드 리뷰를 진행하겠습니다.

1. **시간 복잡도**: 이 쿼리의 시간 복잡도는 O(n)입니다. 여기서 n은 UserActivity 테이블의 행 수를 나타냅니다. 쿼리는 테이블의 모든 행을 스캔하여 사용자별 평균 활동 기간을 계산하기 때문입니다.

2. **공간 복잡도**: 이 쿼리의 공간 복잡도는 O(n)입니다. 쿼리는 사용자별 평균 활동 기간을 계산하여 결과 집합을 반환하기 때문에, 결과 집합의 크기는 사용자 수에 비례합니다.

3. **풀이 접근법**: 이 쿼리에서는 집계 함수(AVG, SUM)와 조건부 표현식(CASE WHEN)을 사용하여 사용자별 평균 활동 기간을 계산합니다. 또한, HAVING 절을 사용하여 조건을 만족하는 사용자만 결과에 포함합니다. 이 접근법은 사용자별 평균 활동 기간을 효율적으로 계산할 수 있습니다.

4. **잘된 점**: 코드는 잘 구조화되어 있으며, 사용된 쿼리 문법은 올바르습니다. 또한, ROUND 함수를 사용하여 평균 활동 기간을 소수점 2자리까지 반올림하여 결과를 깔끔하게 표시합니다.

5. **개선 사항**: 쿼리 자체는 효율적이지만, 테이블의 크기가 매우 큰 경우 쿼리 수행 시간이 길어질 수 있습니다.这种 경우, 인덱스를 생성하여 쿼리 수행 시간을 개선할 수 있습니다. 예를 들어, user_id 열에 인덱스를 생성하여 GROUP BY 절의 수행 시간을 줄일 수 있습니다. 또한, activity_type 열에 인덱스를 생성하여 CASE WHEN 절의 수행 시간을 줄일 수 있습니다. 그러나 인덱스를 생성하는 것은 추가적인 저장 공간을 필요로 하므로, 테이블의 크기와 쿼리 수행 빈도 등을 고려하여 결정해야 합니다.
