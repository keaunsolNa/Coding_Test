# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 13. 오전 10:24:43 |
| Language | oraclesql |
| Runtime | 207 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/analyze-subscription-conversion/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 UserActivity 테이블의 모든 행을 스캔하므로 시간 복잡도는 데이터의 크기(n)에 비례합니다. 데이터베이스의 인덱싱 및 최적화에 따라 실제 수행 시간은 달라질 수 있습니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 사용자 ID와 평균試用 기간을 포함하므로, 결과 집합의 크기는 사용자 수(n)에 비례합니다. 메모리 사용량은 결과 집합의 크기에 따라 달라집니다.

3. **풀이 접근법**: 이 쿼리는 집계 함수(AVG)와 조건문(CASE WHEN)을 사용하여 사용자별 평균試用 기간을 계산합니다. 또한, NVL 함수를 사용하여 NULL 값을 0으로 대체합니다. 이 접근법은 문제를 단순하게 해결하며, 데이터베이스의 집계 함수를 효율적으로 사용합니다.

4. **잘된 점**: 코드는 간결하며, 문제를 명확하게 해결합니다. 사용된 함수와 조건문이 적절하며, 결과를 쉽게 이해할 수 있습니다. 또한, ROUND 함수를 사용하여 평균 값을 소수점 2자리로 반올림하여 결과를 더 읽기 쉽게 만들었습니다.

5. **개선 사항**: 현재 쿼리는 사용자별 평균試用 기간을 계산하며, 추가적인 최적화는 필요하지 않습니다. 그러나, 대량의 데이터를 처리하는 경우, 인덱스를 생성하여 쿼리 성능을 개선할 수 있습니다. 또한, 사용자별 평균試用 기간이 NULL인 경우를 처리하는 로직을 추가할 수 있습니다. 예를 들어, NULL 값을 특정 값으로 대체하거나, 별도의 처리를 수행할 수 있습니다.
