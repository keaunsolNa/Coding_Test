# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 13. 오전 10:28:34 |
| Language | oraclesql |
| Runtime | 235 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/analyze-subscription-conversion/submissions/)

## Code Review

코드 리뷰를 진행하겠습니다.

1. **시간 복잡도**: 이 쿼리의 시간 복잡도는 O(n)입니다. 여기서 n은 UserActivity 테이블의 행 수를 나타냅니다. 쿼리는 테이블의 모든 행을 스캔하여 필요한 데이터를 계산하기 때문입니다.

2. **공간 복잡도**: 이 쿼리의 공간 복잡도는 O(n)입니다. 쿼리는 GROUP BY 절을 사용하여 사용자 ID별로 데이터를 그룹화하고, 각 그룹에 대한 평균 값을 계산합니다. 따라서 쿼리는 중간 결과를 저장하기 위해 추가적인 메모리가 필요합니다.

3. **풀이 접근법**: 이 쿼리는 집계 함수(AVG, SUM)와 조건문(CASE WHEN)을 사용하여 데이터를 분석합니다. 또한 GROUP BY와 HAVING 절을 사용하여 데이터를 그룹화하고 필터링합니다. 이러한 접근법은 관계형 데이터베이스에서 데이터를 분석하는 일반적인 패턴입니다.

4. **잘된 점**: 코드는 읽기 쉽고, 필요한 계산을 명확하게 수행합니다. 또한 ROUND 함수를 사용하여 평균 값을 소수점 2자리까지 반올림하여 결과를 더 읽기 쉽게 합니다.

5. **개선 사항**: 쿼리 자체는 효율적으로 작성되어 있지만, 테이블의 크기가 매우 큰 경우 성능이 저하될 수 있습니다. 이러한 경우, 인덱스를 생성하거나 파티션을 사용하여 데이터를 분할하여 쿼리 성능을 개선할 수 있습니다. 또한, NVL 함수 대신 COALESCE 함수를 사용하여 NULL 값을 대체할 수 있습니다. COALESCE 함수는 여러 개의 인자를 받아서 NULL이 아닌 첫 번째 값을 반환합니다.
