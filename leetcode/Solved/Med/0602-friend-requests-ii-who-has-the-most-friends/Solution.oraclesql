/* Write your PL/SQL query statement below */
SELECT NVL(COUNT(requester_id), 0) num, requester_id 
  FROM RequestAccepted
 GROUP BY requester_id
UNION 
SELECT NVL(COUNT(accepter_id ), 0) num, accepter_id  
  FROM RequestAccepted
 GROUP BY accepter_id 
