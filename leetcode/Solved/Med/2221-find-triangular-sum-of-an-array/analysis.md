# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 14. 오전 11:05:12 |
| Language | java |
| Runtime | 3 ms (Beats 0.0%) |
| Memory | 42.5 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-triangular-sum-of-an-array/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n^2)입니다. 내부 루프에서 배열의 각 원소를 처리하고, 외부 루프에서 이 작업을 반복하기 때문입니다. 배열의 길이가 n이라고 가정할 때, 각 원소에 대한 연산을 n-1번 수행하므로 시간 복잡도는 O(n^2)입니다.

2. **공간 복잡도**: O(n)입니다. 각 반복에서 새로운 배열을 생성하여 이전 배열의 길이보다 하나 적은 길이의 배열을 만듭니다. 따라서 공간 복잡도는 입력 배열의 길이에 비례합니다.

3. **풀이 접근법**: 이 코드는 시뮬레이션을 기반으로 하는 접근법을 사용합니다. 입력 배열의 각 원소에 대해, 다음 원소와의 합을 계산하여 새로운 배열을 생성합니다. 이 작업을 반복하여 최종적으로 하나의 원소만 남을 때까지 반복합니다. 이는 삼각형 합의 특성을 이용한 접근법입니다.

4. **잘된 점**: 코드는 문제의 요구 사항을 충족하는 삼각형 합을 계산하는 로직을 구현했습니다. 또한, 각 반복에서 새로운 배열을 생성하여 이전 결과를 활용하는 방식으로 문제를 해결했습니다.

5. **개선 사항**: 코드의 마지막 부분에서 `return nums[0];`를 사용하고 있습니다. 그러나 이 코드는 문제의 요구 사항을 정확하게 반영하지 않습니다. 대신, `return copyArr[0];`를 사용하여 최종적으로 계산된 삼각형 합의 결과를 반환해야 합니다. 또한, 불필요한 출력문(`System.out.print`, `System.out.println`)을 제거하여 코드를 깔끔하게 유지할 수 있습니다.
