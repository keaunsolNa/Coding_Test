# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 20. 오후 5:41:31 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.6 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/detect-capital/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하여 반복문이 실행되기 때문에 시간 복잡도는 선형입니다. 코드에서 두 번의 반복문이 존재하지만, 각 반복문은 별도로 실행되므로 시간 복잡도는 O(n) + O(n) = O(2n)으로 간주되며, 이는 다시 O(n)으로 단순화됩니다.

2. **공간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하여 추가 배열(`arr2`)이 생성되기 때문에 공간 복잡도는 선형입니다.

3. **풀이 접근법**: 이 코드는 대문자 사용을 체크하기 위해 문자열을 배열로 변환하고, 각 문자를 ASCII 코드로 변환하여 대문자와 소문자를 구분합니다. 이는 기본적인 문자열 처리 알고리즘을 사용한 접근법입니다.

4. **잘된 점**: 코드는 문제의 요구 사항을 이해하고, 대문자와 소문자의 사용을 체크하는 로직을 구현했습니다. 또한, 입력 문자열의 길이가 1인 경우를 별도로 처리하여 예외 상황을 고려했습니다.

5. **개선 사항**: 
- 불필요한 반복문을 줄일 수 있습니다. 현재 코드는 두 번의 반복문을 사용하여 문자열을 체크하지만, 하나의 반복문에서 모든 체크를 완료할 수 있습니다.
- `arr2` 배열의 사용을 줄일 수 있습니다. 대신, 각 문자의 ASCII 코드를 직접 비교하여 대문자와 소문자를 구분할 수 있습니다.
- 변수 이름을 더 의미 있게 바꿀 수 있습니다. 예를 들어, `check`와 `check2`는 더 명확한 이름으로 바꿔서 코드의 가독성을 높일 수 있습니다.
- `System.out.println` 문을 제거할 수 있습니다. 이 문은 디버깅 용도로 사용되었을 것입니다. 최종 코드에서는 이러한 문을 제거하는 것이 좋습니다.
