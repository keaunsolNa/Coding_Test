# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 20. 오후 5:42:38 |
| Language | java |
| Runtime | 11 ms (Beats 3.5%) |
| Memory | 43.3 MB (Beats 45.8%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/detect-capital/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하여 반복문이 실행되기 때문에 시간 복잡도는 선형입니다. 문자열을 두 번 반복하여 처리하지만, 이는 상수배의 차이이므로 여전히 O(n)입니다.

2. **공간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하여 추가 배열을 생성하기 때문에 공간 복잡도도 선형입니다.

3. **풀이 접근법**: 문자열을 배열로 변환하고, 각 문자의 ASCII 값을 계산하여 대문자와 소문자를 구분하는 방법을 사용했습니다. 이는 문자열 처리에서 흔히 사용되는 패턴입니다.

4. **잘된 점**: 코드는 주어진 문제를 해결하는 데 성공했습니다. 또한, 입력 문자열의 길이가 1인 경우를 특별히 처리하여 예외 상황을 고려했습니다.

5. **개선 사항**: 
- 불필요한 변수와 반복문을 줄일 수 있습니다. 예를 들어, `arr2` 배열은 실제로 필요한 정보만을 담고 있으므로, 이를 간소화할 수 있습니다.
- 문자의 ASCII 값을 계산하여 대문자와 소문자를 구분하는 대신, `Character.isUpperCase()`와 `Character.isLowerCase()` 메서드를 사용할 수 있습니다. 이는 코드를 더 읽기 쉽고 직관적으로 만들 수 있습니다.
- 시스템 출력문을 제거하여 코드를 더 깔끔하게 유지할 수 있습니다. 디버깅 목적으로 출력문을 사용한다면, 테스트가 완료된 후에는 제거하는 것이 좋습니다.
- 메서드 이름과 변수 이름을 더 의미 있게 지을 수 있습니다. 예를 들어, `check`와 `check2`는 더 구체적인 이름을 가질 수 있습니다.
