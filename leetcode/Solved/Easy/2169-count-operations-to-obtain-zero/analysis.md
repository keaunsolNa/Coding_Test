# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오후 5:41:03 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 41.6 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/count-operations-to-obtain-zero/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(log(min(num1, num2))) - 유클리드 호제법을 사용하여 두 수를 0으로 만드는 연산의 수를 계산하기 때문에, 두 수 중 작은 수의 로그에 비례하는 시간 복잡도를 가지게 됩니다.

2. **공간 복잡도**: O(1) - 상수 공간만을 사용하여 계산을 수행하기 때문에, 입력 크기에 관계없이 공간 복잡도는 일정합니다.

3. **풀이 접근법**: 유클리드 호제법을 사용하여 두 수를 0으로 만드는 연산의 수를 계산합니다. 큰 수에서 작은 수를 빼는 과정을 반복하여 두 수를 0으로 만듭니다.

4. **잘된 점**: 코드는 간결하고, 유클리드 호제법을 사용하여 효율적으로 두 수를 0으로 만드는 연산의 수를 계산합니다. 또한, 변수의 이름이 명확하여 코드의 가독성이 좋습니다.

5. **개선 사항**: while문 조건이 `num1 == 0 || num2 == 0`로 설정되어 있지만, 실제로 연산을 수행하는 조건은 `num1 != 0 && num2 != 0`입니다. 따라서, while문 조건을 `num1 != 0 && num2 != 0`로 변경하여 코드의 논리적인 일관성을 높일 수 있습니다. 또한, 변수의 초기화와 연산을 수행하는 부분을 분리하여 코드의 가독성을 더 높일 수 있습니다.
