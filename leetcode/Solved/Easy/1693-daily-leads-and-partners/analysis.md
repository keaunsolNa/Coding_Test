# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 16. 오전 11:01:57 |
| Language | mysql |
| Runtime | 504 ms (Beats 97.2%) |
| Memory | 0B (Beats 100.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/daily-leads-and-partners/submissions/)

## Code Review

리뷰 대상 코드는 DailySales 테이블에서 날짜와 자동차 제조사별로 유니크한 리드와 파트너의 수를 계산하는 MySQL 쿼리입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 DailySales 테이블의 모든 행을 스캔해야 하므로 시간 복잡도는 테이블의 행 수에 비례합니다. GROUP BY 연산과 COUNT(DISTINCT) 함수도 추가적인 시간 복잡도를 가졌지만, 이는 이미 O(n)인 스캔 시간에 포함됩니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 날짜와 자동차 제조사별로 그룹화되며, 각 그룹에 대해 유니크한 리드와 파트너의 수를 계산합니다. 결과 집합의 크기는 입력 테이블의 크기에 비례하므로 공간 복잡도도 O(n)입니다.

3. **풀이 접근법**: 이 쿼리는 집계 함수와 GROUP BY 절을 사용하여 문제를 해결합니다. COUNT(DISTINCT) 함수를 사용하여 유니크한 리드와 파트너의 수를 계산하며, GROUP BY 절을 사용하여 날짜와 자동차 제조사별로 결과를 그룹화합니다. 이는 관계형 데이터베이스에서 집계 연산을 수행하는 표준적인 패턴입니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. 필요한 열만 선택하고, COUNT(DISTINCT) 함수를 사용하여 유니크한 리드와 파트너의 수를 정확하게 계산합니다. 또한 GROUP BY 절을 사용하여 결과를 날짜와 자동차 제조사별로 그룹화하여 문제의 요구 사항을满족합니다.

5. **개선 사항**: 쿼리 자체는 효율적이지만, 테이블의 크기가 매우 큰 경우 성능이 저하될 수 있습니다.这种 경우, 인덱스를 생성하여 쿼리 성능을 개선할 수 있습니다. 예를 들어, date_id와 make_name 열에 인덱스를 생성하면 GROUP BY 절과 COUNT(DISTINCT) 함수의 성능을 개선할 수 있습니다. 또한, 쿼리 결과를 캐시하여 동일한 쿼리가 반복적으로 실행될 때 성능을 개선할 수 있습니다.
