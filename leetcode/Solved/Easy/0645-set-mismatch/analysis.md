# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:34:01 |
| Language | java |
| Runtime | 7 ms (Beats 0.0%) |
| Memory | 43.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

리뷰 대상 코드는 LeetCode의 'set-mismatch' 문제를 해결하는 Java 솔루션입니다. 

1. **시간 복잡도**: 이 코드의 시간 복잡도는 O(n log n) + O(n) + O(n) = O(n log n)입니다. 여기서 n은 입력 배열의 크기입니다. 배열을 정렬하는 과정에서 O(n log n)의 시간이 소요되며, 두 번의 반복문을 통해 배열을 순회하는 과정에서 O(n)의 시간이 추가로 소요됩니다.

2. **공간 복잡도**: 이 코드의 공간 복잡도는 O(n)입니다. 입력 배열의 최대값에 기반하여 크기가 동적으로 결정되는 추가 배열이 생성되기 때문입니다. 이 배열의 크기는 입력 배열의 크기와 입력 배열의 최대값에 비례합니다.

3. **풀이 접근법**: 이 코드는 정렬과 카운팅을 기반으로 하는 접근법을 사용합니다. 먼저 입력 배열을 정렬한 후, 각 숫자의 출현 횟수를 카운팅하여 중복과 누락을 식별합니다. 이는 간단하고 직관적인 접근법이지만, 정렬에 의한 시간 복잡도가 상대적으로 높습니다.

4. **잘된 점**: 코드는 비교적 간단하고 이해하기 쉽습니다. 중복과 누락된 숫자를 찾는 로직이 명확하게 구현되어 있습니다. 또한, System.out.println 문을 통해 중간 결과를 출력하여 디버깅을 용이하게 합니다.

5. **개선 사항**: 이 코드의 효율성을 개선하기 위해, 정렬 없이 해시 테이블(HashMap 등)을 사용하여 각 숫자의 출현 횟수를 카운팅할 수 있습니다.这样하면 시간 복잡도를 O(n)으로 줄일 수 있습니다. 또한, 입력 배열의 크기와 최대값에 따라 동적으로 메모리를 할당하는 대신, 고정 크기의 해시 테이블을 사용할 수 있습니다. 이는 공간 복잡도를 더 효율적으로 관리할 수 있도록 합니다. 예를 들어, nums.length + 1 크기의 해시 테이블을 사용하여 1부터 n까지의 숫자를 직접 인덱스로 사용할 수 있습니다.
