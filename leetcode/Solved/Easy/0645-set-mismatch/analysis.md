# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:36:48 |
| Language | java |
| Runtime | 4 ms (Beats 0.0%) |
| Memory | 43.2 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - Arrays.sort() 함수를 사용하여 배열을 정렬하는 데 O(n log n)의 시간이 걸리고, 이후에 배열을 순회하여 요소를 카운팅하는 데 O(n)의 시간이 걸리기 때문에 전체 시간 복잡도는 O(n log n)입니다.

2. **공간 복잡도**: O(n) - 최대값(max)까지의 배열을 생성하여 각 요소의 카운트를 저장하기 때문에 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 문제는 정렬과 카운팅을 사용하여 해결되었습니다. 먼저 배열을 정렬하고, 이후에 각 요소의 카운트를 확인하여 중복된 요소와 누락된 요소를 찾습니다.

4. **잘된 점**: 코드는 비교적 간결하고, 문제의 요구사항을满족합니다. Arrays.sort() 함수를 사용하여 배열을 정렬하고, 이후에 각 요소의 카운트를 확인하여 중복된 요소와 누락된 요소를 찾는 접근법은 간단명료합니다.

5. **개선 사항**: 코드에서 Arrays.sort() 함수를 사용하여 배열을 정렬하는 부분은 시간 복잡도가 O(n log n)입니다. 하지만, 이 문제는 해시 테이블을 사용하여 O(n)의 시간 복잡도로 해결할 수 있습니다. 또한, 코드에서 불필요한 System.out.println() 문이 포함되어 있으므로, 이를 제거하여 코드를 더 깔끔하게 만들 수 있습니다. 또한, 변수명이 더 명확하게 변경되어야 합니다. 예를 들어, 'arr' 변수는 'countArray'로, 'ans' 변수는 'result'로 변경될 수 있습니다.
