# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:31:52 |
| Language | java |
| Runtime | 4 ms (Beats 0.0%) |
| Memory | 43.1 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n)입니다. 이유는 Arrays.sort() 함수를 사용했기 때문입니다. 이 함수는 퀵 정렬 알고리즘을 사용하여 평균적으로 O(n log n)의 시간 복잡도를 가지며, 이는 입력 배열의 크기에 로그를 취한 것에 비례합니다. 또한, 두 개의 for 루프를 사용했지만, 각 루프의 시간 복잡도는 O(n)이며, 상수 곱으로 인해 전체 시간 복잡도는 여전히 O(n log n)입니다.

2. **공간 복잡도**: O(n)입니다. 이유는 추가적으로 arr 배열을 생성했기 때문입니다. 이 배열의 크기는 입력 배열의 최대값에 비례하므로, 최악의 경우에는 O(n)입니다.

3. **풀이 접근법**: 정렬과 카운팅을 사용한 접근법입니다. 먼저 입력 배열을 정렬하고, 이후에 각 숫자의 출현 횟수를 카운팅하여 중복과 누락을 찾습니다. 이는 간단하고 직관적인 접근법이지만, 시간 복잡도는 정렬에 의해 영향을 받습니다.

4. **잘된 점**: 코드는 비교적 간단하고 이해하기 쉽습니다. 또한, 중복과 누락을 찾는 논리는 명확합니다. 변수 이름도 의미가 명확하여 코드의 가독성이 좋습니다.

5. **개선 사항**: Arrays.sort() 함수 대신에 해시 테이블을 사용하여 시간 복잡도를 개선할 수 있습니다. 해시 테이블을 사용하면 각 숫자의 출현 횟수를 O(1) 시간 복잡도로 카운팅할 수 있으므로, 전체 시간 복잡도는 O(n)으로 개선됩니다. 또한, System.out.println() 함수를 사용한 디버깅 코드는 제거하는 것이 좋습니다.
