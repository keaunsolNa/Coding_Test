# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:39:42 |
| Language | java |
| Runtime | 4 ms (Beats 0.0%) |
| Memory | 43.1 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n)입니다. Arrays.sort() 함수를 사용하여 배열을 정렬하는 데 O(n log n)의 시간이 걸리고, 이후에 반복문을 통해 배열을 순회하는 데 O(n)의 시간이 걸리기 때문입니다. 따라서 전체 시간 복잡도는 O(n log n) + O(n) = O(n log n)입니다.

2. **공간 복잡도**: O(n)입니다. 배열을 정렬하고, 추가적인 배열을 생성하여 숫자의 빈도를 계산하기 때문입니다. 생성된 배열의 크기는 입력 배열의 최대값에 비례하므로, 최악의 경우 O(n)입니다.

3. **풀이 접근법**: 이 문제는 정렬과 빈도 계산을 사용하여 해결합니다. 먼저 배열을 정렬하고, 이후에 반복문을 통해 각 숫자의 빈도를 계산하여 중복된 숫자와 누락된 숫자를 찾습니다. 이 접근법은 간단하고 직관적이지만, 시간 복잡도가 높을 수 있습니다.

4. **잘된 점**: 코드는 비교적 간단하고 이해하기 쉽습니다. 또한, 문제의 요구 사항을满족하는 해법을 제공합니다. 특히, Arrays.sort() 함수를 사용하여 배열을 정렬하는 부분은 직관적이고 효율적입니다.

5. **개선 사항**: 코드의 시간 복잡도를 개선할 수 있습니다. 예를 들어, HashSet을 사용하여 중복된 숫자를 찾고, 이후에 반복문을 통해 누락된 숫자를 찾을 수 있습니다. 이 접근법은 시간 복잡도를 O(n)으로 줄일 수 있습니다. 또한, 코드의 가독성을 개선하기 위해 변수 이름을 더 명확하게 지정하고, 불필요한 출력문을 제거할 수 있습니다. 예를 들어, System.out.println()문을 제거하고, 변수 이름을 더 의미 있게 지정할 수 있습니다.
