# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:32:23 |
| Language | java |
| Runtime | 4 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: 이 코드의 시간 복잡도는 O(n log n)입니다. 이는 `Arrays.sort()` 함수를 사용하여 배열을 정렬하는 데 O(n log n)의 시간이 걸리기 때문입니다. 이후 반복문을 통해 배열을 순회하는 데 O(n)의 시간이 걸리지만, 이는 O(n log n)에 비해 무시할 수 있는 수준입니다.

2. **공간 복잡도**: 이 코드의 공간 복잡도는 O(n)입니다. 이는 최대값까지의 배열을 생성하여 각 숫자의 빈도를 저장하기 때문입니다. 배열의 크기는 입력 배열의 최대값에 비례하므로, 최악의 경우에는 O(n)의 공간이 필요합니다.

3. **풀이 접근법**: 이 코드는 정렬과 빈도 계산을 사용하여 문제를 해결합니다. 먼저 배열을 정렬하고, 이후 각 숫자의 빈도를 계산하여 중복된 숫자와 누락된 숫자를 찾습니다. 이는 간단하면서도 효과적인 접근법입니다.

4. **잘된 점**: 코드는 비교적 간단하고 이해하기 쉽습니다. 또한, 문제의 요구 사항을 정확하게 구현하여 올바른 결과를 반환합니다. 특히, 중복된 숫자와 누락된 숫자를 찾는 로직은 명확하고 효율적입니다.

5. **개선 사항**: 코드를 최적화하기 위해, 정렬 대신 해시 테이블을 사용하여 빈도 계산을 수행할 수 있습니다. 이는 시간 복잡도를 O(n)으로 줄일 수 있습니다. 또한, 불필요한 출력 문장을 제거하여 코드를 더 깔끔하게 만들 수 있습니다. 예를 들어, `System.out.println()` 문장을 제거하고, 대신 디버깅을 위한 로깅 메커니즘을 사용할 수 있습니다.
