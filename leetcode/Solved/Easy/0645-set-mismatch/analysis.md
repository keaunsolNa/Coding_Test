# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:40:25 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 43.1 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

리뷰 대상 코드는 LeetCode의 'set-mismatch' 문제를 해결하는 자바 솔루션입니다.

1. **시간 복잡도**: O(n log n) - Arrays.sort() 함수를 사용하여 입력 배열을 정렬하는 데 O(n log n)의 시간이 소요됩니다. 이후 반복문을 통해 각 원소의 빈도수를 계산하고 결과를 찾는 데 O(n)의 시간이 추가로 소요됩니다. 따라서 전체 시간 복잡도는 O(n log n) + O(n) = O(n log n)입니다.

2. **공간 복잡도**: O(n) - 입력 배열의 최대값에 따라 크기가 결정되는 추가 배열을 사용합니다. 최악의 경우, 입력 배열의 크기와 동일한 크기의 추가 배열이 필요하므로 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 솔루션은 정렬과 빈도수 계산을 사용하여 문제를 해결합니다. 먼저 입력 배열을 정렬한 후, 각 원소의 빈도수를 계산하여 중복된 원소와 누락된 원소를 찾습니다. 이 접근법은 간단하고 직관적이지만, 정렬에 의한 시간 복잡도가 상대적으로 높을 수 있습니다.

4. **잘된 점**: 코드는 간결하고 이해하기 쉽습니다. Arrays.sort() 함수를 사용하여 정렬을 간단하게 처리하고, 빈도수 계산을 통해 중복된 원소와 누락된 원소를 효율적으로 찾습니다.

5. **개선 사항**: 정렬에 의한 시간 복잡도를 개선하기 위해, 해시 테이블을 사용하여 빈도수 계산을 수행할 수 있습니다. 이 경우, 시간 복잡도는 O(n)으로 개선될 수 있습니다. 또한, 입력 배열의 크기가 매우 클 경우, 추가 배열의 크기를 최적화하여 공간 복잡도를 개선할 수 있습니다. 예를 들어, 해시 테이블을 사용하여 빈도수 계산을 수행하면, 추가 배열의 크기를 줄일 수 있습니다.
