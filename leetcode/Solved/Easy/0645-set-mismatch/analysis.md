# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:27:28 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 43.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - Arrays.sort() 함수로 인해 시간 복잡도가 O(n log n)입니다. 이는 입력 배열을 정렬하는 데 필요한 시간입니다. 이후 반복문을 통해 배열을 탐색하는 시간은 O(n)입니다.

2. **공간 복잡도**: O(n) - 추가적인 배열을 생성하여 숫자의 빈도를 저장합니다. 이 배열의 크기는 입력 배열의 최대값에 비례하므로 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 문제는 정렬과 빈도 계산을 사용하여 해결됩니다. 먼저 입력 배열을 정렬하고, 이후 빈도 배열을 생성하여 각 숫자의 출현 횟수를 계산합니다. 이를 통해 중복된 숫자와 누락된 숫자를 찾을 수 있습니다.

4. **잘된 점**: 코드는 비교적 간결하고, 문제의 요구 사항을 잘 반영하고 있습니다. 또한, 빈도 계산을 통해 중복된 숫자와 누락된 숫자를 찾는 접근법은 직관적입니다.

5. **개선 사항**: 코드에서 반복문을 통해 빈도 배열을 초기화하는 부분이 있습니다. 이는 불필요한 반복문으로, 빈도 배열을 생성할 때 바로 초기화할 수 있습니다. 또한, Arrays.sort() 함수 대신에 더 효율적인 정렬 알고리즘을 사용할 수 있습니다. 그러나, 이러한 개선 사항은 코드의 가독성과 유지 보수성을 저해하지 않는 범위 내에서 적용되어야 합니다. 또한, 해시 테이블을 사용하여 빈도 계산을 할 수 있습니다. 이는 공간 복잡도를 줄일 수 있습니다.
