# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:34:13 |
| Language | java |
| Runtime | 6 ms (Beats 0.0%) |
| Memory | 43.1 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/set-mismatch/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - Arrays.sort() 함수를 사용하여 배열을 정렬하는 데 O(n log n)의 시간이 소요됩니다. 이후에 반복문을 통해 각 원소의 빈도수를 계산하는 데 O(n)의 시간이 추가로 소요됩니다.

2. **공간 복잡도**: O(n) - max값까지의 배열을 생성하여 각 원소의 빈도수를 계산하기 위해 O(n)의 공간이 필요합니다.

3. **풀이 접근법**: 이 문제는 정렬과 빈도수 계산을 사용하여 해결되었습니다. 먼저 입력 배열을 정렬한 후, 각 원소의 빈도수를 계산하여 중복된 원소와 누락된 원소를 찾습니다.

4. **잘된 점**: 코드는 간결하고 이해하기 쉽습니다. Arrays.sort() 함수를 사용하여 배열을 정렬하는 것은 좋은 접근법입니다. 또한, 빈도수 계산을 통해 중복된 원소와 누락된 원소를 찾는 방법은 직관적입니다.

5. **개선 사항**: 코드에서 System.out.println()과 System.out.print()문을 사용하여 디버깅 정보를 출력하고 있습니다. 이러한 문들은 실제 코드에서 제거하는 것이 좋습니다. 또한, 변수명이 좀 더 명확하게 지어졌으면 좋았을 것입니다. 예를 들어, 'arr' 대신 'frequency'로 지었으면 더 이해하기 쉬웠을 것입니다. 마지막으로, 입력 배열이 null이나 빈 배열인 경우를 처리하는 코드가 추가되어야 합니다.
