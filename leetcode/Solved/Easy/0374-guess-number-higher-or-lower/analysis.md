# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 1:37:27 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/guess-number-higher-or-lower/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(log n) - 이 코드는 이진 탐색 알고리즘을 사용하여 숫자를猜测합니다. 각 반복에서 검색 범위가 절반으로 줄어들기 때문에 시간 복잡도는 O(log n)입니다.

2. **공간 복잡도**: O(1) - 이 코드는 추가적인 공간을 사용하지 않기 때문에 공간 복잡도는 O(1)입니다. 변수는 상수 개수만큼 사용되며, 입력 크기에 따라 증가하지 않습니다.

3. **풀이 접근법**: 이진 탐색 알고리즘을 사용하여 풀이합니다. `guess` 함수를 사용하여 현재猜测한 숫자가 정답보다 높은지 낮은지 확인하고, 그에 따라 다음猜测를 조정합니다.

4. **잘된 점**: 코드는 비교적 간결하고, 이진 탐색 알고리즘을 효과적으로 사용하여 시간 복잡도를 최적화했습니다. 또한, `guess` 함수를 사용하여 풀이를 간단하게 구현했습니다.

5. **개선 사항**: 현재 코드는 이진 탐색을 사용하여 효율적으로 풀이하고 있지만, 초기값을 `n`으로 설정하여 최초의猜测를 `n`으로 시작합니다. 더 좋은 접근법은 초기값을 `1`과 `n`의 중간값으로 설정하여 최초의猜测를 더 좁은 범위에서 시작하는 것입니다. 또한, 변수명과 주석을 더 자세하게 작성하여 코드의 가독성을 높일 수 있습니다. 예를 들어, `num` 변수를 `currentGuess`로 이름을 변경하여 변수의 역할을 더 명확하게 나타낼 수 있습니다.
