# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 1:37:18 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/guess-number-higher-or-lower/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(log n) - 이진 탐색 알고리즘을 사용하여 숫자를 찾기 때문에, 탐색 범위가 반복적으로 절반으로 줄어들기 때문에 시간 복잡도는 로그 형태로 감소합니다.

2. **공간 복잡도**: O(1) - 추가적인 공간을 사용하지 않기 때문에, 공간 복잡도는 상수형태로 유지됩니다.

3. **풀이 접근법**: 이진 탐색 알고리즘을 사용하여 문제를 해결했습니다. 이 알고리즘은 중간 값을 기준으로 탐색 범위를 반복적으로 나누어 탐색하는 방식입니다. 하지만, 제공된 코드에서는 완전한 이진 탐색 알고리즘이 아닌, 중간 값을 계산하는 방식이 다소 다르게 구현되어 있습니다.

4. **잘된 점**: 코드는 비교적 간결하고, 문제의 요구사항을 이해하기 쉽게 작성되어 있습니다. 또한, 중간 값을 계산하여 탐색 범위를 좁히는 방식은 좋은 접근법입니다.

5. **개선 사항**: 현재 코드에서 중간 값을 계산하는 방식은 완전한 이진 탐색 알고리즘과 다소 차이가 있습니다. 더 효율적인 방법은 두 개의 포인터 (low, high)를 사용하여 탐색 범위를 좁히는 방식입니다. 예를 들어, `low`와 `high`를 초기화하여 중간 값을 계산하고, `guess` 함수의 반환 값을 기준으로 `low`와 `high`를 업데이트하는 방식입니다. 또한, `System.out.println(num);` 문은 디버깅 목적으로 사용된 것으로 보이는데, 실제 제출 시에는 제거하는 것이 좋습니다. 또한, 반환값은 `num`이 아닌 `n`으로 설정되어 있습니다. 이는 잘못된 반환값으로, 실제로 찾은 숫자인 `num`을 반환해야 합니다.
