# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 12:14:55 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.6 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/maximum-number-of-balloons/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하는 시간 복잡도를 가지고 있습니다. 이는 문자열을 한 번만 순회하여 각 문자의 빈도를 계산하기 때문입니다.

2. **공간 복잡도**: O(1) - 문자의 빈도를 저장하는 해시맵의 크기는 입력 문자열의 길이와는 무관하게 'b', 'a', 'l', 'o', 'n' 5개의 키만을 저장하므로 상수 시간 복잡도를 가집니다.

3. **풀이 접근법**: 해시맵을 사용하여 각 문자의 빈도를 계산하고, "balloon"이라는 단어의 각 문자가 필요한 최소 빈도수를 계산하여 그 중 가장 작은 값을 반환하는 알고리즘을 사용했습니다. 이는 문자열에서 특정 패턴의 최대 반복 횟수를 찾는 문제에 적합한 접근법입니다.

4. **잘된 점**: 코드는 간결하고, 문제를 명확하게 이해하여 필요한 변수와 계산만을 사용했습니다. 또한, `getOrDefault` 메서드를 사용하여 null 체크를 피하고 코드를 더 읽기 쉽게 만들었습니다.

5. **개선 사항**: 현재 코드는 이미 매우 효율적이고 간결합니다. 그러나, 변수 이름을 더 의미 있게 변경하여 코드의 가독성을 높일 수 있습니다. 예를 들어, `b`, `a`, `l`, `o`, `n`을 `balloonB`, `balloonA` 등으로 변경할 수 있습니다. 또한, 변수에 대한 주석을 추가하여 코드의 의도를 더 명확하게 설명할 수 있습니다.
