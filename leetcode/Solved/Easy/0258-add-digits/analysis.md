# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 4. 오후 3:50:37 |
| Language | java |
| Runtime | 3 ms (Beats 0.0%) |
| Memory | 42.5 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/add-digits/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(logN) - 입력 숫자의 자릿수에 비례하는 시간 복잡도를 가지며, 각 자릿수를 처리하는 내부 반복문이 입력 숫자의 크기에 따라 반복되기 때문입니다. 숫자를 10으로 나눌 때마다 자릿수가 줄어들기 때문에, 로그 시간 복잡도가 발생합니다.

2. **공간 복잡도**: O(1) - 코드에서 사용하는 변수의 개수는 입력 크기에 따라 변하지 않으므로, 상수 공간 복잡도를 가집니다. 변수 `target`와 `hab`은 입력 숫자의 크기에 따라 동적으로 할당되지 않기 때문입니다.

3. **풀이 접근법**: 이 코드는 수학적 접근법을 사용하여 숫자의 각 자릿수를 더하는 알고리즘을 구현했습니다. 내부 반복문을 사용하여 숫자의 각 자릿수를 추출하고, 이를 더한 결과를 다시 반복적으로 처리하여 최종적으로 한 자릿수의 숫자를 얻습니다.

4. **잘된 점**: 코드는 문제를 명확하게 이해하고, 이를 단순한 반복문을 사용하여 구현했습니다. 또한, 변수 이름이 직관적이고, 코드의 구조가 명확하여 이해하기 쉽습니다.

5. **개선 사항**: 코드에서 사용하는 `System.out.println` 문은 디버깅 목적으로 사용되는 것으로 보입니다. 실제로 제출할 때는 이러한 문장을 제거하는 것이 좋습니다. 또한, 더 나은 변수 이름을 사용하거나, 주석을 추가하여 코드의 의도를 더 명확하게 설명할 수 있습니다. 예를 들어, `hab` 변수의 이름을 `sum`으로 변경하거나, `target` 변수의 이름을 `number`로 변경할 수 있습니다. 또한, 코드를 더 간결하게 만들기 위해 수학적 성질을 이용하여 한 줄의 코드로 해결할 수 있습니다. 예를 들어, `return (num - 1) % 9 + 1;`와 같이 구현할 수 있습니다.
