# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 4. 오후 3:49:17 |
| Language | java |
| Runtime | 3 ms (Beats 0.0%) |
| Memory | 42.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/add-digits/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(logN) - 입력 숫자의 자릿수에 따라 반복문이 실행되므로, 숫자의 자릿수가 증가할수록 반복 횟수도 증가합니다. 하지만 숫자의 자릿수는 로그 계열로 증가하므로, 시간 복잡도는 O(logN)입니다.

2. **공간 복잡도**: O(1) - 변수는 상수 개수만 사용하므로, 입력 크기에 따라 메모리 사용량이 증가하지 않습니다.

3. **풀이 접근법**: 이 문제는 수학적 접근법을 사용하여 해결할 수 있습니다. 숫자의 각 자릿수를 더해가며, 결과가 10보다 작을 때까지 반복합니다. 이 알고리즘은 수의 각 자릿수를 더하는 과정을 반복하여, 최종적으로 한 자리 수를 얻는다는 아이디어에 기반합니다.

4. **잘된 점**: 코드는 문제를 명확하게 이해하고, 해당 문제를 해결하는 알고리즘을 구현했습니다. 또한, 변수 이름이 의미 있게 사용되어 코드의 가독성이 좋습니다.

5. **개선 사항**: 코드에서 `System.out.println` 문을 사용하여 디버깅을 위한 출력을 하고 있습니다. 하지만, 실제로 제출할 때는 이러한 문장을 제거하여야 합니다. 또한, 더 나은 변수 이름을 사용하여 코드의 가독성을 높일 수 있습니다. 예를 들어, `hab` 변수는 `sum` 또는 `total`로 이름을 변경할 수 있습니다. 마지막으로, 더 효율적인 알고리즘을 사용할 수 있습니다. 예를 들어, 숫자의 각 자릿수를 더하는 대신, 수학적 성질을 이용하여 한 자리 수를 바로 계산할 수 있습니다. 이는 `(num - 1) % 9 + 1`로 계산할 수 있습니다. 이는 숫자의 각 자릿수를 더하는 과정을 생략할 수 있으므로, 더 효율적인 해결 방법입니다.
