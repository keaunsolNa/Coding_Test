# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 4. 오후 3:48:58 |
| Language | java |
| Runtime | 3 ms (Beats 0.0%) |
| Memory | 42.1 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/add-digits/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(log n) - 이 알고리즘은 입력 숫자의 각 자릿수를 더하는 과정을 반복합니다. 숫자의 자릿수가 줄어들 때마다 반복 횟수가 줄어들기 때문에, 시간 복잡도는 숫자의 자릿수에 비례합니다. 숫자의 자릿수는 log n에 비례하므로, 시간 복잡도는 O(log n)입니다.

2. **공간 복잡도**: O(1) - 이 알고리즘은 입력 숫자와 중간 결과를 저장하기 위해 상수 개수의 변수만 사용합니다. 따라서 공간 복잡도는 O(1)입니다.

3. **풀이 접근법**: 이 알고리즘은 수학적 접근법을 사용합니다. 숫자의 각 자릿수를 더하는 과정을 반복하여, 최종적으로 한 자릿수의 숫자를 얻습니다. 이 알고리즘은 "디지털 루트" 또는 "재귀적 합" 알고리즘으로도 알려져 있습니다.

4. **잘된 점**: 코드는 간결하고, 변수 이름이 명확합니다. 또한, 중간 결과를 출력하는 코드가 포함되어 있으므로, 디버깅이 용이합니다.

5. **개선 사항**: 코드에서 `System.out.println`문을 제거하여, 불필요한 출력을 방지할 수 있습니다. 또한, 변수 이름을 더 의미 있게 변경하여, 코드의 가독성을 높일 수 있습니다. 예를 들어, `hab` 변수를 `sum`으로 변경할 수 있습니다. 또한, 더 효율적인 알고리즘을 사용할 수 있습니다. 예를 들어, 숫자의 디지털 루트를 계산하는 공식 `digitalRoot = (num - 1) % 9 + 1`을 사용할 수 있습니다. 이 공식은 한 자릿수의 숫자를 바로 계산할 수 있으므로, 반복적인 합 계산을 피할 수 있습니다.
