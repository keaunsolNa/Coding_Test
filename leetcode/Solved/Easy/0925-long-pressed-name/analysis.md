# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 12. 오후 4:13:44 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.6 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/long-pressed-name/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n + m)입니다. 여기서 n은 `name`의 길이, m은 `typed`의 길이입니다. 두 문자열을 각각 한 번씩 반복하여 처리하기 때문에 이 시간 복잡도가 발생합니다.

2. **공간 복잡도**: O(n + m)입니다. 두 문자열의 문자별 빈도수를 저장하기 위해 사용된 `HashMap`이 공간 복잡도의 주된 원인입니다.

3. **풀이 접근법**: 해시맵을 사용하여 각 문자의 빈도수를 계산하고, 두 문자열의 빈도수를 비교하는 방식입니다. 이는 문자열 비교 문제에서 흔히 사용되는 접근법 중 하나입니다.

4. **잘된 점**: 코드는 비교적 간결하고, 해시맵을 사용하여 빈도수 계산을 효율적으로 처리했습니다. 또한, 변수명이 명확하여 코드의 가독성이 좋습니다.

5. **개선 사항**: 현재 코드는 두 문자열의 순서를 고려하지 않고, 단순히 빈도수만 비교합니다. 그러나 `long-pressed-name` 문제는 순서도 중요합니다. 따라서, 두 문자열을 순서대로 비교하는 방식으로 접근해야 합니다. 예를 들어, 두 개의 포인터를 사용하여 `name`과 `typed`를 순서대로 비교할 수 있습니다. 또한, `System.out.println` 문은 제거하고, 대신 적절한 예외 처리나 반환 값을 사용하는 것이 좋습니다. 마지막으로, `check` 변수의 사용이 불필요하며, 코드를 더 간결하게 만들 수 있습니다.
