# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026-05-10 18:41:20 |
| Language | oraclesql |
| Runtime | 992 ms (Beats 22.7%) |
| Memory | 0B (Beats 100.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/employee-bonus/submissions/)

## Code Review

코드 리뷰如下입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 Employee 테이블과 Bonus 테이블을 조인해야 하므로, 조인하는 테이블의 크기에 따라 시간 복잡도가 달라집니다. 일반적으로 테이블의 크기가 n이라고 가정했을 때, 조인 연산의 시간 복잡도는 O(n)입니다.

2. **공간 복잡도**: O(n) - 쿼리 결과를 저장하기 위해 필요한 공간의 크기도 테이블의 크기에 따라 달라집니다. 일반적으로 테이블의 크기가 n이라면, 공간 복잡도는 O(n)입니다.

3. **풀리 접근법**: 이 쿼리는 LEFT JOIN과 WHERE 절을 사용하여 Bonus 테이블에 Bonus 값이 NULL이거나 1000 미만인 사원들의 이름과 보너스 값을 가져옵니다. 조인과 필터링을 조합하여 문제를 해결하는 간단한 접근법입니다.

4. **잘된 점**: 이 쿼리는 문제의 요구 사항을 정확하게 구현하고 있습니다. 또한, 코드가 간결하여 이해하기 쉽습니다. LEFT JOIN을 사용하여 Bonus 테이블에 해당하는 레코드가 없는 사원들의 이름도 가져올 수 있습니다.

5. **개선 사항**: 쿼리 자체는 최적화된 것으로 보이지만, 테이블의 인덱스를 확인하여 JOIN 작업을 최적화할 수 있습니다. 예를 들어, Employee 테이블의 empId 열과 Bonus 테이블의 empId 열에 인덱스를 생성하여 조인 작업의 성능을提高할 수 있습니다. 또한, 큰 테이블을 다루는 경우에는 파티션닝과 같은 데이터베이스 최적화 기법을 적용할 수 있습니다.
