# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026-05-31 16:07:31 |
| Language | java |
| Runtime | 4 ms (Beats 52.8%) |
| Memory | 45.2 MB (Beats 33.7%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/third-maximum-number/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도** — O(n)으로, 입력 배열의 크기에 비례하는 시간을 소요합니다. 이는 단 한번의 반복문을 통해 최대 3개의 값을 찾기 때문입니다.

2. **공간 복잡도** — O(1)로, 입력 배열의 크기와 상관없이 상수 만큼의 추가 공간을 사용합니다. 이는 최대 3개의 변수를 사용하여 최대값을追跡하기 때문입니다.

3. **풀이 접근법** — 단일 반복문을 사용하여 입력 배열을 순회하며, 최대 3개의 값을 유지하는 방식의 알고리즘을 사용했습니다. 이는 문제의 제약 조건에 적합한 간단하고 효율적인 방법입니다.

4. **잘된 점** — 코드는 가독성이 좋으며, 변수명이 명확하고 간결합니다. 또한, null 체크와 같은 예외 상황을 잘 처리했습니다. 문제의 요구 사항에ตรง하는 간단한 해결책을 제시했습니다.

5. **개선 사항** — 현재 구현된 코드는 문제의 요구 사항을 충족합니다. 하지만, 더 효율적인 방법으로는 Arrays.stream()을 사용하여 스트림 연산을 수행하는 것입니다. 예를 들어, `Arrays.stream(nums).distinct().sorted().skip(nums.length - 3).max()`와 같은 방법을 사용하여 더 간결한 코드를 구현할 수 있습니다. 하지만, 문제의 난이도와 요구 사항을 고려하면 현재 구현된 코드도 충분히 적절합니다.
