# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 3:02:59 |
| Language | oraclesql |
| Runtime | 268 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/average-selling-price/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: 이 쿼리의 시간 복잡도는 O(n log n)입니다. 이는 데이터베이스에서 데이터를 읽고 조인하는 과정에서 발생하는 시간 복잡도입니다. 특히, `GROUP BY` 절과 `SUM` 함수를 사용하여 데이터를 집계하는 과정에서 시간 복잡도가 증가합니다.

2. **공간 복잡도**: 이 쿼리의 공간 복잡도는 O(n)입니다. 이는 데이터베이스에서 데이터를 읽고 처리하는 과정에서 발생하는 공간 복잡도입니다. 특히, 중간 결과를 저장하는 데 필요한 공간이 증가합니다.

3. **풀이 접근법**: 이 쿼리에서는 기본적인 관계형 데이터베이스 질의 접근법을 사용했습니다. 즉, `JOIN`을 사용하여 두 개의 테이블을 연결하고, `WHERE` 절을 사용하여 데이터를 필터링하고, `GROUP BY` 절을 사용하여 데이터를 집계했습니다. 또한, `ROUND` 함수와 `SUM` 함수를 사용하여 평균 가격을 계산했습니다.

4. **잘된 점**: 이 쿼리는 간결하고 읽기 쉽습니다. 또한, 필요한 데이터만을 읽고 처리하기 때문에 효율적인 쿼리입니다. 특히, `BETWEEN` 절을 사용하여 날짜 범위를 지정하는 부분이 잘 구현되어 있습니다.

5. **개선 사항**: 이 쿼리는 이미 비교적 효율적입니다. 그러나, 데이터가 매우大量인 경우, 인덱스를 생성하여 쿼리 성능을 개선할 수 있습니다. 또한, `EXISTS`나 `IN`을 사용하여 서브쿼리를 최적화할 수 있습니다. 그러나, 이러한 최적화는 데이터베이스의 특성과 쿼리 패턴에 따라 달라질 수 있습니다. 따라서, 실제 데이터베이스에서 쿼리를 실행하여 성능을 측정하고, 필요한 경우에만 최적화를 수행하는 것이 좋습니다.
