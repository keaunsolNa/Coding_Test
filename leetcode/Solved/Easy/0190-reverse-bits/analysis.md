# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 27. 오후 1:13:47 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 41.9 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/reverse-bits/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**  
   - **O(1)**  
   - `Integer.reverse(int)` 메서드는 내부적으로 32비트 정수를 고정된 횟수만큼 비트 이동 및 마스크 연산을 수행하므로 입력 크기에 관계없이 상수 시간에 동작합니다.

2. **공간 복잡도**  
   - **O(1)**  
   - 추가적인 힙 메모리나 배열을 할당하지 않으며, 메서드 스택도 고정된 몇 개의 로컬 변수만 사용합니다. 따라서 사용 메모리는 입력 크기와 무관한 상수 공간입니다.

3. **풀이 접근법**  
   - Java 표준 라이브러리인 `Integer.reverse(int)` 를 그대로 활용한 **비트 조작** 접근법입니다.  
   - 이 메서드는 “divide and conquer” 스타일로 16‑bit, 8‑bit, 4‑bit, 2‑bit, 1‑bit 단위로 마스크와 시프트를 반복해 비트를 뒤집는 구현을 가지고 있습니다.

4. **잘된 점**  
   - **간결함**: 한 줄 코드로 문제를 해결했으며, 가독성이 뛰어납니다.  
   - **신뢰성**: JDK에 최적화된 구현을 사용하므로 버그 위험이 거의 없습니다.  
   - **성능**: 표준 라이브러리 구현은 하드웨어 수준 최적화가 적용될 가능성이 높아 실제 실행 속도가 빠릅니다.

5. **개선 사항**  
   - **제한 조건 고려**: LeetCode에서는 `unsigned int`(32비트) 입력을 가정합니다. Java의 `int`는 부호가 있기 때문에, 입력이 음수일 경우에도 비트 패턴은 동일하지만 반환값이 부호 있는 정수로 해석됩니다. 문제에서 요구하는 출력이 부호 없는 형태라면 `return Integer.reverse(n) >>> 0;` 와 같이 `>>>` 연산으로 부호 비트를 0으로 채워 반환하는 것이 명확합니다. (하지만 실제 채점에서는 `int` 그대로도 통과합니다.)  
   - **직접 구현 연습**: 면접이나 알고리즘 학습 목적이라면 직접 비트 마스크와 시프트를 이용한 구현을 연습하는 것이 좋습니다. 예를 들어 5단계 마스크/시프트 방식이나 루프를 이용한 32번 반복 방법을 구현하면 알고리즘 이해도가 높아집니다.  
   - **주석 추가**: 현재는 한 줄 구현이라 주석이 없으므로, `// Use built‑in method to reverse 32‑bit integer` 정도의 간단한 설명을 달아두면 코드 리뷰 시 의도가 바로 파악됩니다.  

**요약**  
전반적으로 매우 간결하고 효율적인 해결책이며, 시간·공간 복잡도 모두 최적 수준(O(1))입니다. 다만 부호 처리와 학습 목적을 고려해 약간의 보완(주석, 부호 명시)이나 직접 구현 연습을 추가하면 더욱 완성도 높은 풀이가 됩니다.
