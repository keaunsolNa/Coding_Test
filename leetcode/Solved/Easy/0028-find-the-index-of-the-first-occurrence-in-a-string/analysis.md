# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 21. 오후 12:15:42 |
| Language | java |
| Runtime | 0 ms (Beats 100.0%) |
| Memory | 42.9 MB (Beats 82.8%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/submissions/)

## Code Review

리뷰 대상 코드는 자바로 작성된 문자열 검색 함수입니다. 

1. **시간 복잡도**: 이 함수의 시간 복잡도는 O(n*m)입니다. 여기서 n은 `haystack`의 길이, m은 `needle`의 길이입니다. `indexOf` 메서드는 내부적으로 문자열을 순회하며 패턴을 찾는 알고리즘을 사용하기 때문에 이러한 시간 복잡도가 발생합니다.

2. **공간 복잡도**: 공간 복잡도는 O(1)입니다. 이 함수는 추가적인 공간을 할당하지 않기 때문에 입력 문자열 외에 상수 크기의 공간만 사용합니다.

3. **풀이 접근법**: 사용된 알고리즘은 자바의 내장 `indexOf` 메서드를 이용한 문자열 검색입니다. 이 메서드는 `haystack` 문자열에서 `needle` 문자열의 첫 번째 발생 위치를 찾습니다. 이는 KMP(Knuth-Morris-Pratt) 알고리즘과 같은 문자열 검색 알고리즘을 간단하게 구현한 것입니다.

4. **잘된 점**: 코드는 매우 간결하고 읽기 쉽습니다. 자바의 내장 메서드를 사용하여 문자열 검색을 간단하게 구현했습니다. 이는 코드의 가독성과 유지보수를 높입니다.

5. **개선 사항**: 이 문제는 Easy 난이도이므로 현재 구현은 충분히 적절합니다. 그러나 더 최적화된 접근법으로 KMP 알고리즘을 직접 구현할 수 있습니다. KMP 알고리즘은 문자열 검색을 위해 선처리된 테이블을 사용하여 검색 속도를 개선합니다. 하지만 이러한 최적화는 더 복잡한 코드를 의미하므로, 문제의 난이도와 요구 사항에 따라 적절한 접근법을 선택해야 합니다.
