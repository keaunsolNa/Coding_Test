# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 11. 오후 3:09:48 |
| Language | java |
| Runtime | 3 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/delete-columns-to-make-sorted/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n\*m^2)입니다. 여기서 n은 문자열의 길이이고, m은 문자열의 개수입니다. 코드에서 두 개의 중첩된 반복문이 있습니다. 첫 번째 반복문은 문자열의 각 문자를 처리하고, 두 번째 반복문은 각 문자열을 처리합니다. 또한, 내부에 또 다른 반복문이 있습니다. 따라서 시간 복잡도는 O(n\*m^2)입니다.

2. **공간 복잡도**: O(n\*m)입니다. 코드에서 2차원 배열 `arr`을 사용하여 문자열의 각 문자를 저장합니다. 따라서 공간 복잡도는 O(n\*m)입니다.

3. **풀이 접근법**: 코드에서는 각 문자열의 문자를 비교하여 정렬된 상태를 확인합니다. 하지만, 이 접근법은 문제의 요구사항을 정확하게 반영하지 않습니다. 문제는 삭제할 열의 최소 개수를 찾으라고 요구합니다. 따라서, 더 효율적인 접근법은 각 열의 문자를 비교하여 정렬된 상태를 확인하는 것입니다.

4. **잘된 점**: 코드에서는 문자열의 각 문자를 숫자로 변환하여 비교하는 부분이 잘 구현되어 있습니다. 또한, 코드의 구조는 비교적 간단하고 이해하기 쉽습니다.

5. **개선 사항**: 코드의 시간 복잡도를 개선할 수 있습니다. 현재 코드에서는 각 문자열의 문자를 비교하여 정렬된 상태를 확인합니다. 하지만, 더 효율적인 접근법은 각 열의 문자를 비교하여 정렬된 상태를 확인하는 것입니다. 또한, 코드에서는 불필요한 반복문을 사용하고 있습니다. 예를 들어, `System.out.println`문을 사용하여 배열의 내용을 출력하는 부분은 삭제할 수 있습니다. 또한, 변수의 이름을 더 명확하게 변경하여 코드의 가독성을 높일 수 있습니다. 예를 들어, `l`과 `n`을 `rowCount`와 `columnCount`로 변경할 수 있습니다.
