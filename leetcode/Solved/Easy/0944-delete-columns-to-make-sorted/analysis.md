# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 11. 오후 3:04:58 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.5 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/delete-columns-to-make-sorted/submissions/)

## Code Review

코드 리뷰를 진행하겠습니다.

1. **시간 복잡도**: O(n^2 * l) - 이 코드는 2중 반복문을 사용하여 각 문자열의 문자를 비교합니다. 여기서 n은 문자열의 길이, l은 문자열의 개수입니다. 각 문자열의 문자를 비교하는 과정에서 시간 복잡도가 증가합니다.

2. **공간 복잡도**: O(n * l) - 코드는 2차원 배열을 사용하여 문자열의 문자를 저장합니다. 배열의 크기는 문자열의 길이와 문자열의 개수에 비례하므로 공간 복잡도는 O(n * l)입니다.

3. **풀이 접근법**: 이 코드는 각 문자열의 문자를 비교하여 정렬된 상태를 유지하기 위해 삭제해야 할 열의 개수를 계산합니다. 그러나 코드의 로직이 복잡하고, 일부 변수의 이름이 명확하지 않습니다. 또한, 코드는 각 문자열의 문자를 2차원 배열에 저장하고, 배열을 반복적으로 순회하여 비교합니다.

4. **잘된 점**: 코드는 문제를 해결하기 위해 노력했으며, 일부 로직은 올바르게 구현되었습니다. 예를 들어, 코드는 문자열의 길이가 1인 경우를 처리하여 불필요한 계산을 피합니다.

5. **개선 사항**: 코드의 로직을 단순화하고, 변수의 이름을 명확하게 변경할 수 있습니다. 또한, 코드는 불필요한 반복문을 사용하여 시간 복잡도를 증가시킵니다. 예를 들어, 코드는 각 문자열의 문자를 비교하여 정렬된 상태를 유지하기 위해 삭제해야 할 열의 개수를 계산할 때, 불필요한 반복문을 사용합니다. 이를 개선하기 위해, 코드는 각 열의 문자를 비교하여 정렬된 상태를 유지하기 위해 삭제해야 할 열의 개수를 계산할 수 있습니다. 또한, 코드는 Java의 내장 함수를 사용하여 문자열의 비교를 간단하게 할 수 있습니다. 예를 들어, `String.compareTo()` 함수를 사용하여 두 문자열을 비교할 수 있습니다.
