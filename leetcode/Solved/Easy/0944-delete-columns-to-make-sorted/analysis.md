# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 11. 오후 3:06:23 |
| Language | java |
| Runtime | 2 ms (Beats 0.0%) |
| Memory | 42.6 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/delete-columns-to-make-sorted/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n*m^2)입니다. 여기서 n은 문자열의 길이이고, m은 문자열의 개수입니다. 코드에서 두 개의 중첩된 반복문이 사용되며, 각 반복문에서 문자열의 길이와 문자열의 개수만큼 반복됩니다. 또한, 내부 반복문에서 문자열을 비교하기 위해 또 다른 반복문이 사용됩니다.

2. **공간 복잡도**: O(n*m)입니다. 코드에서 2차원 배열을 사용하여 문자열의 각 문자를 저장합니다. 배열의 크기는 문자열의 길이와 문자열의 개수에 비례합니다.

3. **풀이 접근법**: 코드에서는 문자열을 2차원 배열로 변환하여 각 문자를 비교합니다. 그리고 각 문자열의 문자를 비교하여 오름차순인지 아닌지 확인합니다. 이 접근법은 기본적으로 문자열의 각 문자를 비교하여 정렬된 문자열을 만드는 것입니다.

4. **잘된 점**: 코드에서는 문자열을 2차원 배열로 변환하여 각 문자를 비교하는 부분이 잘 구현되어 있습니다. 또한, 코드에서는 각 문자열의 문자를 비교하여 오름차순인지 아닌지 확인하는 부분도 잘 구현되어 있습니다.

5. **개선 사항**: 코드에서는 불필요한 반복문이 사용됩니다. 예를 들어, 코드에서는 각 문자열의 문자를 비교하여 오름차순인지 아닌지 확인하는 부분에서 불필요한 반복문이 사용됩니다. 또한, 코드에서는 2차원 배열을 사용하여 문자열의 각 문자를 저장합니다. 하지만, 코드에서는 문자열의 각 문자를 비교하여 오름차순인지 아닌지 확인하는 부분에서 1차원 배열을 사용하여도 충분합니다. 따라서, 코드를 최적화하기 위해 불필요한 반복문과 2차원 배열을 제거할 수 있습니다. 또한, 코드에서는 더 효율적인 알고리즘을 사용할 수 있습니다. 예를 들어, 코드에서는 각 문자열의 문자를 비교하여 오름차순인지 아닌지 확인하는 부분에서 동적 프로그래밍을 사용할 수 있습니다.
