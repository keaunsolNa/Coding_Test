# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 11. 오후 2:53:05 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.6 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/delete-columns-to-make-sorted/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n*m) - 여기서 n은 문자열의 길이, m은 문자열 배열의 크기입니다. 각 문자열을 한 번씩 반복하여 문자열 내의 문자를 비교하기 때문입니다.

2. **공간 복잡도**: O(1) - 입력 문자열 배열을 제외하고는 추가적인 공간을 사용하지 않습니다. 따라서 상수 공간 복잡도입니다.

3. **풀이 접근법**: 이 문제는 각 문자열을 왼쪽에서 오른쪽으로扫描하며, 각 문자가 이전 문자보다 작으면 삭제할 컬럼의 수를 증가시킵니다. 이는 기본적인 문자열 비교 알고리즘을 사용합니다.

4. **잘된 점**: 코드는 간결하고, 변수 이름이 명확하여 이해하기 쉽습니다. 또한, 시간 복잡도와 공간 복잡도도 적절하게 관리되어 있습니다.

5. **개선 사항**: 현재 코드는 각 문자열을 개별적으로 검사하여 삭제할 컬럼의 수를 계산합니다. 그러나, 문제의 조건을 만족하는 최소 컬럼 삭제 수를 찾는 것이 목표입니다. 따라서, 모든 문자열을 동시에 비교하여 컬럼 단위로 삭제할 수 있는지 확인하는 방식으로 접근할 수 있습니다. 예를 들어, 모든 문자열의 첫 번째 문자를 비교하여 정렬이 깨지는지 확인하고, 그렇다면 해당 컬럼을 삭제하는 방식입니다. 이러한 방식으로 코드를 개선하면 더 효율적인 해결책을 찾을 수 있습니다.
