# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 11. 오후 3:10:30 |
| Language | java |
| Runtime | 4 ms (Beats 0.0%) |
| Memory | 42.9 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/delete-columns-to-make-sorted/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n^2 * l)입니다. 여기서 n은 문자열의 길이, l은 문자열의 개수입니다. 코드에서 두 개의 중첩된 반복문이 사용되어 시간 복잡도가 O(n^2 * l)가 됩니다. 또한, 문자열의 각 문자를 배열에 저장하는 과정도 O(n * l)의 시간 복잡도를 가집니다.

2. **공간 복잡도**: O(n * l)입니다. 코드에서 2차원 배열을 사용하여 문자열의 각 문자를 저장하기 때문에 공간 복잡도가 O(n * l)가 됩니다.

3. **풀이 접근법**: 코드에서는 각 문자열의 문자를 비교하여 정렬된 문자열을 만들기 위해 삭제해야 할 열의 개수를 계산합니다. 하지만, 코드의 로직이 복잡하고, 일부 변수의 이름이 명확하지 않습니다. 또한, 코드에서 사용된 알고리즘은 단순한 비교를 통해 삭제해야 할 열의 개수를 계산합니다.

4. **잘된 점**: 코드에서는 문자열의 각 문자를 배열에 저장하여 비교하기 쉽게 만들었습니다. 또한, 코드에서 System.out.println을 사용하여 중간 결과를 출력하여 디버깅하기 쉽게 만들었습니다.

5. **개선 사항**: 코드의 로직을 단순화하고, 변수의 이름을 명확하게 만들면 코드의 가독성이 향상됩니다. 또한, 코드에서 사용된 알고리즘은 단순한 비교를 통해 삭제해야 할 열의 개수를 계산하기 때문에, 더 효율적인 알고리즘을 사용하여 시간 복잡도를 개선할 수 있습니다. 예를 들어, 코드에서 각 열의 문자를 비교하여 정렬된 문자열을 만들기 위해 삭제해야 할 열의 개수를 계산할 때, 한 번의 반복문으로 계산할 수 있습니다. 또한, 코드에서 사용된 2차원 배열을 사용하지 않고, 문자열의 각 문자를 직접 비교하여 삭제해야 할 열의 개수를 계산할 수 있습니다.
