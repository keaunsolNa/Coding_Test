# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 11. 오후 1:04:00 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/minimum-depth-of-binary-tree/submissions/)

## Code Review

이진 트리의 최소 깊이를 구하는 문제의 코드 리뷰입니다.

1. **시간 복잡도**: 이 코드의 시간 복잡도는 O(N)입니다. 여기서 N은 트리의 노드 개수입니다. 이유는 각 노드를 한 번씩 방문하기 때문입니다. 재귀 호출로 인해 모든 노드를 방문하여 최소 깊이를 계산합니다.

2. **공간 복잡도**: 이 코드의 공간 복잡도는 O(H)입니다. 여기서 H는 트리의 높이입니다. 이유는 재귀 호출 스택의 최대 크기가 트리의 높이와 동일하기 때문입니다. 트리가 균형이 잘 잡혀 있다면 H = log(N)입니다. 그러나 트리가 한쪽으로 치우친 경우 H = N이 될 수 있습니다.

3. **풀이 접근법**: 이 코드는 재귀를 이용한 깊이 우선 탐색(DFS, Depth-First Search)을 사용하여 트리의 최소 깊이를 계산합니다. 각 노드의 왼쪽과 오른쪽 자식 노드의 깊이를 계산하여 더 작은 값을 선택합니다.

4. **잘된 점**: 코드는 간결하고 이해하기 쉽습니다. 재귀 함수를 사용하여 트리의 깊이를 계산하는 로직이 명확하게 표현되어 있습니다. 또한, null 체크를 통해 예외 처리를 잘 수행하고 있습니다.

5. **개선 사항**: 이 코드는 트리의 모든 노드를 방문하여 최소 깊이를 계산합니다. 그러나 이진 트리의 최소 깊이를 구하는 문제는 레벨 순회(BFS, Breadth-First Search)를 사용하여 더 효율적으로 해결할 수 있습니다. BFS를 사용하면 트리의 최소 깊이를 더 빠르게 계산할 수 있습니다. 특히, 트리가 매우 깊거나 노드가 많은 경우에 유용합니다. 또한, 재귀 호출 대신 반복문을 사용하여 스택 오버플로우를 피할 수 있습니다.
