# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 16. 오후 1:49:45 |
| Language | java |
| Runtime | 2 ms (Beats 20.7%) |
| Memory | 42.8 MB (Beats 62.4%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/defanging-an-ip-address/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n)입니다. 여기서 n은 주어진 IP 주소의 길이입니다. `replaceAll` 메서드는 문자열을 일차적으로扫描하여 패턴을 찾고, 이를 대체합니다. 따라서 문자열의 길이에 비례하는 시간이 소요됩니다.

2. **공간 복잡도**: O(n)입니다. `replaceAll` 메서드는 새로운 문자열을 생성하여 반환합니다. 따라서 원래 문자열과 동일한 길이의 새로운 문자열이 생성되므로, 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 풀이에서는 정규식 패턴을 사용하여 IP 주소의 점을 대체합니다. `replaceAll` 메서드는 Java에서 정규식 패턴을 지원하며, "\\."는 점을 의미하는 정규식 패턴입니다. "[.]"는 점을 문자열로 대체하는 패턴입니다.

4. **잘된 점**: 코드는 매우 간결하고, 문제를 명확하게 해결합니다. 정규식 패턴을 사용하여 문제를 간단하게 해결하였습니다.

5. **개선 사항**: 코드는 이미 충분히 최적화되어 있습니다. 그러나, 다른 접근법으로는 문자열의 각 문자를扫描하여 점을 발견할 때마다 대체 문자열을 추가하는 방법이 있습니다. 하지만, 이러한 접근법은 코드를 더 복잡하게 만들고, 정규식 패턴을 사용하는 것보다 효율적이지 않습니다. 따라서, 현재의 코드는 이미 충분히 좋은 풀이입니다.
