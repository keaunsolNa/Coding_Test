/* Write your PL/SQL query statement below */
SELECT TO_CHAR(sell_date, 'YYYYMMDD') AS sell_date
     , COUNT(DISTINCT product) num_sold
     , LISTAGG(DISTINCT product, ',') WITHIN GROUP (ORDER BY product) products
  FROM Activities
 GROUP BY sell_date
 ORDER BY sell_date
