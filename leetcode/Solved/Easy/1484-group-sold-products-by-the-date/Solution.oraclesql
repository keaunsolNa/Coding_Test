/* Write your PL/SQL query statement below */
SELECT TO_CHAR(sell_date, 'YYYYMMDD')
     , count(product) num_sold
     , LISTAGG(product, ',') WITHIN GROUP (ORDER BY sell_date)  products
  FROM Activities     
 GROUP BY sell_date 