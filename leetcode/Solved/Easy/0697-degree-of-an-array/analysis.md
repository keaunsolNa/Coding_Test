# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 27. 오후 3:28:30 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/degree-of-an-array/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n) - 이 코드는 주어진 배열을 두 번 순회합니다. 첫 번째 순회에서는 각 요소의 빈도수를 계산하고, 두 번째 순회에서는 각 요소의 첫 번째와 마지막 인덱스를 찾습니다. 따라서 시간 복잡도는 O(n)입니다.

2. **공간 복잡도**: O(n) - 이 코드는 HashMap, ArrayList, 그리고 원본 배열의 복사본을 사용합니다. 따라서 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 코드는 해시 맵을 사용하여 각 요소의 빈도수를 계산하고, 가장 빈도수가 높은 요소를(들)를 찾습니다. 이후에 각 요소의 첫 번째와 마지막 인덱스를 찾아서, 가장 짧은 부분 배열의 길이를 계산합니다. 이 접근법은 문제를 효율적으로 해결할 수 있습니다.

4. **잘된 점**: 이 코드는 문제를 명확하게 이해하고, 해시 맵을 사용하여 각 요소의 빈도수를 효율적으로 계산했습니다. 또한, 코드는 읽기 쉽고, 변수명이 명확하여 이해하기 쉽습니다.

5. **개선 사항**: 이 코드는 이미 효율적이지만, 일부 최적화를 할 수 있습니다. 예를 들어, 첫 번째와 마지막 인덱스를 찾을 때, 별도의 리스트를 만들지 말고, 바로 첫 번째와 마지막 인덱스를 계산하여 결과를 업데이트할 수 있습니다. 또한, 일부 변수명이 더 명확해질 수 있습니다. 예를 들어, `list` 변수는 `maxFrequencyElements`로 이름을 바꾸어 더 명확하게 할 수 있습니다. 또한, `ans` 변수는 `minLength`로 이름을 바꾸어 더 명확하게 할 수 있습니다.
