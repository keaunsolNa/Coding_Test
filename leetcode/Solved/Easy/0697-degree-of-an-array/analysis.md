# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 27. 오후 3:20:45 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/degree-of-an-array/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n) - 이 코드는 두 번의 반복문을 통해 배열을 순회하므로, 시간 복잡도는 배열의 크기(n)에 비례합니다. 첫 번째 반복문에서는 각 요소의 빈도수를 계산하고, 두 번째 반복문에서는 시작 인덱스를 찾습니다.
2. **공간 복잡도**: O(n) - 코드는 해시 맵을 사용하여 각 요소의 빈도수를 저장하므로, 공간 복잡도는 배열의 크기(n)에 비례합니다. 최악의 경우, 모든 요소가 고유할 수 있으므로 해시 맵의 크기는 n이 됩니다.
3. **풀이 접근법**: 이 코드는 해시 맵을 사용하여 각 요소의 빈도수를 계산하고, 가장 빈도수가 높은 요소를 찾는 접근법을 사용합니다. 이후, 가장 빈도수가 높은 요소의 시작과 끝 인덱스를 찾습니다.
4. **잘된 점**: 코드는 간결하고, 해시 맵을 사용하여 빈도수를 계산하는 부분은 효율적입니다. 또한, 변수 이름이 명확하여 코드를 이해하기 쉽습니다.
5. **개선 사항**: 두 번째 반복문을 사용하여 시작 인덱스를 찾는 부분은 불필요합니다. 첫 번째 반복문에서 이미 각 요소의 빈도수를 계산하고, 가장 빈도수가 높은 요소를 찾았으므로, 시작 인덱스를 찾기 위해 별도의 반복문을 사용할 필요가 없습니다. 대신, 첫 번째 반복문에서 가장 빈도수가 높은 요소의 첫 번째 발생 인덱스를 함께 계산할 수 있습니다. 예를 들어, `map`에 요소의 첫 번째 발생 인덱스를 함께 저장할 수 있습니다.
