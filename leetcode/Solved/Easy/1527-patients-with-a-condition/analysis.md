# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 5:36:21 |
| Language | oraclesql |
| Runtime | 235 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/patients-with-a-condition/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 Patients 테이블의 모든 행을 스캔해야 하므로 시간 복잡도는 데이터의 크기와 비례합니다. LIKE 연산자로 인해 전체 테이블을 스캔해야 하므로 효율적인 인덱스를 사용할 수 없습니다.

2. **공간 복잡도**: O(1) - 쿼리는 추가적인 공간을 사용하지 않으므로 공간 복잡도는 상수입니다. 결과 집합은 Patients 테이블의 일부만을 포함하므로 추가적인 메모리 사용은 없습니다.

3. **풀이 접근법**: 이 쿼리는 LIKE 연산자를 사용하여 conditions 컬럼에서 특정 문자열을 검색합니다. 이는 문자열 검색에 일반적으로 사용되는 패턴입니다. OR 연산자를 사용하여 두 가지 조건을 모두 만족하는 행을 검색합니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. LIKE 연산자를 사용하여 조건을 만족하는 행을 검색하는 것은 올바른 접근법입니다. 또한, 필요한 컬럼만을 선택하여 불필요한 데이터 전송을 피합니다.

5. **개선 사항**: 쿼리의 성능을 개선하기 위해 인덱스를 생성할 수 있습니다. 그러나 LIKE 연산자로 인해 효율적인 인덱스를 사용할 수 없습니다. 대안적으로, conditions 컬럼을 별도의 테이블로 분리하여 정규화할 수 있습니다. 이를 통해 더 효율적인 인덱스를 생성할 수 있습니다. 또한, 정규식 함수를 사용하여 더 복잡한 문자열 검색을 수행할 수 있습니다. 그러나 이러한 접근법은 더 복잡한 설계와 유지보수를 요구하므로慎重하게 고려해야 합니다.
