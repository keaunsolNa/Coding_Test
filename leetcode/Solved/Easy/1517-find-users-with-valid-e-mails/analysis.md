# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 3:18:13 |
| Language | oraclesql |
| Runtime | 231 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-users-with-valid-e-mails/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 Users 테이블의 모든 행을 스캔하므로 시간 복잡도는 테이블의 행 수에 비례합니다. 즉, 테이블의 크기가 커질수록 쿼리 수행 시간도 증가합니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 Users 테이블의 모든 행을 포함하므로 공간 복잡도도 테이블의 행 수에 비례합니다. 즉, 테이블의 크기가 커질수록 결과 집합의 크기도 증가합니다.

3. **풀이 접근법**: 이 쿼리는 단순한 프로젝션과 문자열 함수를 사용하여 이메일 주소를 분리합니다. SUBSTR와 INSTR 함수를 사용하여 이메일 주소의 접두사와 접미사를 추출합니다. 이 접근법은 간단하고 직관적이며, Oracle SQL의 내장 함수를 효과적으로 사용합니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. 필요한 열만 선택하고, 이메일 주소를 분리하는 로직은 명확하게 표현되어 있습니다. 또한, Oracle SQL의 표준 함수를 사용하여 이메일 주소를 처리하므로 코드의 이식성이 높습니다.

5. **개선 사항**: 쿼리 자체는 충분히 최적화되어 있지만, 추가적인 필터링이나 정렬이 필요할 경우 WHERE나 ORDER BY 절을 추가할 수 있습니다. 또한, 이메일 주소의 유효성을 검사하는 추가적인 로직이 필요할 수 있습니다. 예를 들어, 이메일 주소의 형식이 올바른지, 도메인이 존재하는지 등에 대한 검사가 필요할 수 있습니다. 이러한 추가적인 로직은 쿼리나 프로시저에서 구현할 수 있습니다.
