# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 28. 오후 3:38:00 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.4 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/total-distance-traveled/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**  
   - **O(1)**  
   - 입력값 `mainTank`, `additionalTank`에 대해 단 한 번의 산술 연산과 `Math.min` 호출만 수행하므로 입력 크기에 관계없이 실행 시간이 일정합니다.

2. **공간 복잡도**  
   - **O(1)**  
   - 추가적인 배열, 리스트, 재귀 호출 등 동적 메모리를 사용하지 않으며, 메서드 내부에 선언되는 변수는 기본형(`int`) 몇 개뿐이므로 상수 공간만 사용합니다.

3. **풀이 접근법**  
   - **수학적 식 직접 계산**  
   - 문제는 “주유소에서 1리터당 10km를 주행하고, 5리터당 추가 연료통 1개를 교환할 수 있다”는 규칙을 이용해 총 주행 거리를 구하는 것입니다.  
   - `mainTank` 리터로 기본 주행 거리 `mainTank * 10`을 얻고, 교환 가능한 추가 연료통 수는 `min(additionalTank, mainTank / 5)` 로 계산합니다. 교환된 연료통당 10km를 더해 최종 거리를 구합니다.  
   - 이와 같이 한 줄의 수식으로 문제를 해결하는 **수학적 귀납** 접근법을 사용했습니다.

4. **잘된 점**  
   - **간결함**: 불필요한 반복문이나 조건문 없이 한 줄의 식으로 정답을 도출했습니다. 가독성이 높고 실수 가능성이 적습니다.  
   - **정확한 로직**: `mainTank / 5` 로 교환 가능한 최대 추가 연료통 수를 구하고, 실제 보유한 `additionalTank`와 비교해 최소값을 취함으로써 문제 조건을 정확히 반영했습니다.  
   - **표준 라이브러리 활용**: `Math.min`을 사용해 가독성을 높이고, 직접 구현할 필요 없이 안전하게 최소값을 구했습니다.

5. **개선 사항**  
   - **변수명 명확화**: 현재 메서드 파라미터 이름이 `mainTank`, `additionalTank` 로 직관적이지만, 교환 가능한 추가 연료통 수를 의미하는 중간값을 변수에 저장하면 가독성이 더 향상됩니다. 예시)  
     ```java
     int exchangeable = Math.min(additionalTank, mainTank / 5);
     return (mainTank + exchangeable) * 10;
     ```  
   - **주석 추가**: 한 줄 식이라도 각 부분이 무엇을 의미하는지 간단히 주석을 달면, 코드를 처음 보는 사람도 이해하기 쉽습니다.  
   - **입력 검증(선택 사항)**: LeetCode에서는 입력이 항상 유효하지만, 실제 서비스 코드라면 `mainTank`·`additionalTank`가 음수가 아닌지 검증하는 로직을 넣을 수 있습니다.  

전반적으로 **시간·공간 모두 O(1)** 로 최적의 효율을 보이며, 코드도 매우 간결하고 정확합니다. 위의 작은 가독성 개선 정도만 적용한다면 더욱 완성도 높은 구현이 될 것입니다.
