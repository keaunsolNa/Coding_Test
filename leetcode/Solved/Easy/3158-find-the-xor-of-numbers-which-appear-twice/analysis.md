# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 4. 오후 4:02:11 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-the-xor-of-numbers-which-appear-twice/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 코드는 입력 배열을 한 번만 순회하므로 시간 복잡도는 선형입니다. 여기서 n은 입력 배열의 크기를 나타냅니다.

2. **공간 복잡도**: O(1) - 코드는 고정 크기(51)의 배열을 사용하므로 공간 복잡도는 상수입니다. 입력 크기에 따라 동적으로 메모리를 할당하지 않습니다.

3. **풀이 접근법**: 이 코드는 해시 테이블과 비트 조작을 사용하여 중복된 숫자의 XOR를 찾습니다. 각 숫자를 인덱스로 사용하여 배열에 카운트를 증가시키고, 카운트가 2가 되면 해당 숫자를 XOR합니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. 중복된 숫자의 XOR를 찾는 로직이 명확하게 구현되어 있습니다. 또한, 코드는 문제의 제약 조건을 잘 반영하여 구현되어 있습니다.

5. **개선 사항**: 코드는 이미 충분히 최적화되어 있습니다. 하지만, 더 일반적인 해결책을 찾고 싶다면, 입력 배열의 크기에 따라 동적으로 크기를 조정하는 배열을 사용할 수 있습니다. 또한, Java의 내장 해시 테이블인 `HashMap`을 사용하여 더 유연한 해결책을 구현할 수 있습니다. 그러나, 이러한 변경은 코드의 복잡도를 증가시키므로, 현재의 구현이 충분히 간결하고 효율적입니다.
