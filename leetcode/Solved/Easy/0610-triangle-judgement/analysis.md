# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 6. 16. 오후 9:13:40 |
| Language | mysql |
| Runtime | 103 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/triangle-judgement/submissions/)

## Code Review

코드 리뷰를 진행하겠습니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 Triangle 테이블의 모든 행을 스캔하므로 시간 복잡도는 데이터의 크기와 직접적으로 관련이 있습니다. GREATEST 함수는 상수 시간에 실행되므로 전체 쿼리의 시간 복잡도에 영향을 미치지 않습니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 원본 테이블의 크기와 동일한 크기의 결과 집합을 생성하므로 공간 복잡도도 데이터의 크기와 직접적으로 관련이 있습니다.

3. **풀이 접근법**: 이 쿼리는 간단한 선택과 집계 함수를 사용합니다. GREATEST 함수를 사용하여 각 행의 x, y, z 값 중 최대값을 찾습니다. 이 접근법은 문제의 요구 사항을 직접적으로 해결하며, 효율적이고 읽기 쉬운 코드를 제공합니다.

4. **잘된 점**: 코드는 매우 간결하고 읽기 쉽습니다. GREATEST 함수의 사용은 코드를 더 간단하고 이해하기 쉽게 만듭니다. 또한, 필요한 열만 선택하여 불필요한 데이터를 로드하지 않으므로 효율적입니다.

5. **개선 사항**: 현재 쿼리는 문제의 요구 사항을 충족하지만, 추가적인 최적화나 대안적 접근법은 없습니다. 그러나, 실제로 이 쿼리를 사용하는 경우, Triangle 테이블에 적절한 인덱스를 생성하여 쿼리 성능을 개선할 수 있습니다. 하지만, 이 문제는 단순한 쿼리 최적화 문제이므로, 현재의 쿼리는 충분히 효율적입니다.
