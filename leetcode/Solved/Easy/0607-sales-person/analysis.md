# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 27. 오전 8:56:51 |
| Language | mysql |
| Runtime | 123 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/sales-person/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: 이 쿼리의 시간 복잡도는 O(n^3)입니다. 이유는 세 개의 테이블(SalesPerson, Orders, Company)을 조인하기 때문입니다. 각 테이블의 레코드 수를 각각 n1, n2, n3이라고 가정하면, 조인 연산의 시간 복잡도는 n1 * n2 * n3이 됩니다. 그러나 실제 시간 복잡도는 데이터베이스의 인덱스 설정, 조인 순서 등에 따라 달라질 수 있습니다.

2. **공간 복잡도**: 이 쿼리의 공간 복잡도는 O(n)입니다. 이유는 결과 집합에 포함되는 레코드 수는 Company 테이블의 레코드 수(n3)와 동일하기 때문입니다. 즉, 결과 집합의 크기는 Company 테이블의 크기에 비례합니다.

3. **풀이 접근법**: 이 쿼리에서는 내부 조인(inner join) 알고리즘을 사용하여 세 개의 테이블을 연결했습니다. 조인 조건은 SalesPerson 테이블의 sales_id와 Orders 테이블의 sales_id, Orders 테이블의 com_id와 Company 테이블의 com_id를 기준으로 합니다. 또한, Company 테이블의 이름이 'RED'가 아닌 레코드만을 선택했습니다.

4. **잘된 점**: 이 쿼리는 문제의 요구 사항을 명확하게 이해하고, 필요한 조인과 필터링을 수행하여 결과를 도출했습니다. 또한, 쿼리 문법이 올바르고, 읽기 쉽습니다.

5. **개선 사항**: 이 쿼리는 이미 비교적 간결하고 효율적으로 작성되어 있습니다. 그러나, 다음과 같은 점을 고려할 수 있습니다.
- 인덱스 설정: 조인 조건과 WHERE 절의 조건에 사용되는 열에 인덱스를 설정하면 쿼리 성능을 향상시킬 수 있습니다.
- 조인 순서: 조인 순서는 쿼리 성능에 영향을 줄 수 있습니다. 데이터베이스의 최적화 알고리즘에 따라 자동으로 조인 순서를 결정하지만, 필요에 따라 수동으로 조인 순서를 지정할 수 있습니다.
- 쿼리 최적화: 데이터베이스의 쿼리 최적화 도구를 사용하여 쿼리 계획을 분석하고, 성능을 향상시킬 수 있습니다.
