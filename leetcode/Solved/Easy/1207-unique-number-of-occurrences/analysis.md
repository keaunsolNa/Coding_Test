# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 12:48:35 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/unique-number-of-occurrences/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 코드의 시간 복잡도는 O(n)입니다. 이는 배열을 한 번 순회하여 각 요소의 빈도수를 계산하고, 또 한 번 순회하여 빈도수의 중복 여부를 확인하기 때문입니다. 여기서 n은 입력 배열의 크기입니다.

2. **공간 복잡도**: O(n) - 공간 복잡도 또한 O(n)입니다. 이는 최악의 경우, 모든 요소가 고유한 경우에 해시 맵과 리스트에 n개의 요소를 저장해야 하기 때문입니다.

3. **풀이 접근법**: 이 코드는 해시 맵과 리스트를 사용하여 문제를 해결합니다. 먼저, 해시 맵을 사용하여 각 요소의 빈도수를 계산하고, 그 다음에 리스트를 사용하여 빈도수의 중복 여부를 확인합니다. 이는 문제의 요구 사항을 충족하는 간단한 접근법입니다.

4. **잘된 점**: 코드는 쉽게 이해할 수 있으며, 변수명이 명확하여 코드의 의도를 쉽게 파악할 수 있습니다. 또한, 코드는 문제의 요구 사항을 충족하는 올바른 로직을 가지고 있습니다.

5. **개선 사항**: 코드에서 `valList.contains(num)`를 사용하여 빈도수의 중복 여부를 확인합니다. 그러나 이 방법은 리스트의 크기가 커질수록 시간 복잡도가 O(n)으로 증가하여 비효율적입니다. 대신, 해시 집합을 사용하여 빈도수를 중복 없이 저장하고, 그 크기를 확인하여 중복 여부를 판단할 수 있습니다. 예를 들어, `Set<Integer> valSet = new HashSet<>();`를 사용하여 빈도수를 추가하고, `valSet.size() != map.size()`를 사용하여 중복 여부를 확인할 수 있습니다.
