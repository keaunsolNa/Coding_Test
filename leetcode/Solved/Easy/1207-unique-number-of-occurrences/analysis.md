# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 12:48:49 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/unique-number-of-occurrences/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 코드의 시간 복잡도는 O(n)입니다. 이는 입력 배열을 한 번 순회하여 빈도 수를 계산하고, 또 다른 순회를 통해 중복된 빈도 수를 확인하기 때문입니다. 여기서 n은 입력 배열의 크기입니다.

2. **공간 복잡도**: O(n) - 공간 복잡도 또한 O(n)입니다. 이는 입력 배열의 모든 요소가 유일할 경우, 빈도 수를 저장하는 맵과 리스트의 크기가 n이 될 수 있기 때문입니다.

3. **풀이 접근법**: 이 코드는 해시 맵과 리스트를 사용하여 문제를 해결합니다. 먼저, 해시 맵을 사용하여 각 요소의 빈도 수를 계산하고, 이후 리스트를 사용하여 빈도 수의 중복 여부를 확인합니다. 이는 문제의 요구 사항을 만족하는 간단하면서도 효과적인 접근법입니다.

4. **잘된 점**: 코드는 문제의 요구 사항을 명확하게 이해하고, 이를 만족하는 로직을 구현했습니다. 또한, 해시 맵과 리스트를 사용하여 효율적인 시간 복잡도를 달성했습니다.

5. **개선 사항**: 코드에서 `valList.contains(num)`를 사용하여 중복된 빈도 수를 확인하고 있습니다. 그러나, 이는 리스트의 크기가 커질수록 시간 복잡도가 O(n)으로 증가할 수 있습니다. 대신, 해시 집합을 사용하여 빈도 수를 저장하고, 중복 여부를 확인할 수 있습니다. 예를 들어, `Set<Integer> valSet = new HashSet<>();`를 선언하고, `if (!valSet.add(num)) return false;`를 사용하여 중복된 빈도 수를 확인할 수 있습니다. 이는 시간 복잡도를 O(1)으로 개선할 수 있습니다.
