# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026-05-26 21:48:44 |
| Language | java |
| Runtime | 12 ms (Beats 99.3%) |
| Memory | 53.9 MB (Beats 26.9%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/design-hashset/submissions/)

## Code Review

해당 자바 코드는 해시셋을 구현하는 간단한解决方案입니다. 

1. **시간 복잡도**: O(1) - 해시셋의 모든 연산(add, remove, contains)이 배열의 인덱스에 직접 접근하므로, 시간 복잡도는 평균적으로 O(1)입니다.

2. **공간 복잡도**: O(1) - 데이터 구조의 크기가 최대 크기(MAX_SIZE)로 고정되어 있으므로, 공간 복잡도는 O(1)입니다. 하지만 실제로는 MAX_SIZE 만큼의 공간을 항상 사용하므로, 사용하지 않는 공간이 많을 수 있습니다.

3. **풀이 접근법**:해시셋을 구현하기 위해 boolean형 배열을 사용했습니다. add, remove, contains 연산은 모두 배열의 인덱스에 직접 접근하여 수행합니다. 이는 해시셋의 특성을 최대한 활용하여 간단하고 효율적인 해결 방법입니다.

4. **잘된 점**: 코드는 매우 간단하고 읽기 쉽습니다. 배열을 사용하여 해시셋을 구현하는 아이디어는 좋은 접근법입니다. 또한, MAX_SIZE를 정의하여 최대 크기를 제한하는 방식은 코드의 안정성을 높입니다.

5. **개선 사항**: 해시셋의 최대 크기를 고정하여 공간을 낭비할 수 있습니다. 실제로 사용되는 키의 수에 따라 동적으로 크기를 조정하는 방식이 더 효율적일 수 있습니다. 하지만, 이러한 방식은 복잡도가 증가할 수 있으므로, trade-off를 고려해야 합니다. 또한, 코드에 주석이나 설명을 추가하여 이해하기 쉽게 할 수 있습니다.
