# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 4:16:17 |
| Language | oraclesql |
| Runtime | 254 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/list-the-products-ordered-in-a-period/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - 이 쿼리는 조인 연산과 그룹화 연산을 포함하고 있습니다. 일반적으로 Oracle에서 조인 연산의 시간 복잡도는 O(n log n)이며, 그룹화 연산의 시간 복잡도도 O(n log n)입니다. 따라서 전체 쿼리의 시간 복잡도는 O(n log n)입니다.

2. **공간 복잡도**: O(n) - 쿼리 결과를 저장하기 위한 공간이 필요합니다. 결과 집합의 크기는 입력 데이터의 크기에 비례하므로 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 쿼리는 조인과 집계를 사용하여 풀이합니다. Products 테이블과 Orders 테이블을 조인하여 각 제품의 주문량을 계산하고, 그 결과를 제품 이름으로 그룹화하여 집계합니다. 이는 관계형 데이터베이스에서 데이터를 처리하는 일반적인 패턴입니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. 필요한 열만 선택하고, 조인과 그룹화가 올바르게 사용되어 있습니다. 또한, 쿼리가 명확하고 이해하기 쉽습니다.

5. **개선 사항**: 현재 쿼리는 특정 기간 동안 주문된 제품을 조회하는 문제를 해결하기 위해 작성되었습니다. 그러나 쿼리에는 기간 조건이 없습니다. 따라서 기간 조건을 추가하여 쿼리를 완성해야 합니다. 예를 들어, `WHERE T2.order_date BETWEEN 'start_date' AND 'end_date'`와 같은 조건을 추가할 수 있습니다. 또한, 조인 연산을 명시적으로 지정하여 코드의 가독성을 높일 수 있습니다. 예를 들어, `INNER JOIN`을 사용하여 조인 연산을 명시적으로 지정할 수 있습니다.
