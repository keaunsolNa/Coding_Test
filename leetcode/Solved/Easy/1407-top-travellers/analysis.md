# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 5:15:05 |
| Language | oraclesql |
| Runtime | 314 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/top-travellers/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - 이 쿼리에서는 사용자와 라이드 데이터를 조인하고, 그룹화하고, 정렬하기 때문에 시간 복잡도는 데이터의 크기(n)에 비례하여 증가합니다. 특히, 정렬 연산이 가장 많은 시간을 차지하므로 시간 복잡도는 O(n log n)입니다.

2. **공간 복잡도**: O(n) - 쿼리 결과를 저장하기 위해 필요한 공간은 데이터의 크기(n)에 비례하여 증가합니다. 특히, 중간 결과를 저장하기 위해 필요한 공간이 가장 많으므로 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 쿼리에서는 조인, 그룹화, 집계 함수를 사용하여 사용자별 이동 거리를 계산합니다. 특히, OUTER JOIN을 사용하여 모든 사용자를 포함하고, NVL 함수를 사용하여 NULL 값을 0으로 대체합니다. 또한, SUM 함수를 사용하여 이동 거리를 계산하고, ORDER BY 절을 사용하여 결과를 정렬합니다.

4. **잘된 점**: 이 쿼리는 문제를 명확하게 이해하고, 필요한 데이터를 효과적으로 조회합니다. 특히, OUTER JOIN과 NVL 함수를 사용하여 모든 사용자를 포함하고, NULL 값을 처리하는 방식이 좋습니다.

5. **개선 사항**: 이 쿼리는 이미 효율적으로 작성되어 있지만, 일부 최적화를 고려할 수 있습니다. 예를 들어, 인덱스를 생성하여 조인과 정렬 연산을 가속화할 수 있습니다. 또한, 쿼리 최적화 도구를 사용하여 쿼리 플랜을 분석하고, 최적화할 수 있습니다. 또한, 사용자별 이동 거리를 계산하는 함수를 생성하여 쿼리를 더 간결하게 만들 수 있습니다.
