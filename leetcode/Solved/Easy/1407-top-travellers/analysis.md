# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 5:22:33 |
| Language | oraclesql |
| Runtime | 509 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/top-travellers/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - 이 쿼리는 사용자와乗車 기록을 조인하고, 그룹화하고, 정렬하는 작업을 수행합니다. 데이터베이스의 쿼리 최적화에 따라 실제 시간 복잡도는 달라질 수 있지만, 일반적으로는 O(n log n)으로 생각할 수 있습니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 사용자 수만큼의 행을 반환하므로, 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 쿼리는 Outer Join과 Group By를 사용하여 사용자별로 이동한 거리를 계산합니다. 사용된 알고리즘은 기본적인 관계형 데이터베이스 쿼리입니다.

4. **잘된 점**: 코드는 간결하고, 읽기 쉽습니다. Outer Join을 사용하여 사용자와 乗車 기록을 조인하는 부분이 잘 구현되어 있습니다. 또한, NVL 함수를 사용하여 NULL 값을 0으로 대체하는 부분도 잘 처리되어 있습니다.

5. **개선 사항**: 쿼리 최적화를 위해 인덱스를 생성하는 것이 좋습니다. 예를 들어, USERS 테이블의 id 열과 RIDES 테이블의 user_id 열에 인덱스를 생성하면 쿼리 성능을 개선할 수 있습니다. 또한, 데이터베이스의 버전에 따라는 EXISTS나 IN을 사용하여 쿼리를 작성하는 것이 더 효율적일 수 있습니다. 그러나, 이 쿼리는 이미 충분히 간결하고 효율적이므로, 큰 개선의 필요는 없습니다.
