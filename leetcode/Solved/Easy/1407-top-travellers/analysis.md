# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 5:12:56 |
| Language | oraclesql |
| Runtime | 341 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/top-travellers/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: 이 쿼리의 시간 복잡도는 O(n log n)입니다. 여기서 n은 사용자와 라이드의 총 개수입니다. 이유는 쿼리에서 GROUP BY와 ORDER BY가 사용되기 때문입니다. GROUP BY는 사용자별로 그룹화하고, ORDER BY는 거리와 이름으로 정렬하기 때문에 시간 복잡도가 증가합니다.

2. **공간 복잡도**: 이 쿼리의 공간 복잡도는 O(n)입니다. 쿼리에서 사용되는 임시 테이블이나 변수의 크기는 입력 데이터의 크기에 비례하기 때문입니다.

3. **풀이 접근법**: 이 쿼리에서는 JOIN과 집계 함수(SUM)를 사용하여 사용자별로 이동 거리를 계산합니다. 또한 ORDER BY를 사용하여 결과를 정렬합니다. 이는 관계형 데이터베이스에서 데이터를 처리하는 일반적인 패턴입니다.

4. **잘된 점**: 이 쿼리는 문제를 명확하게 이해하고, 필요한 데이터를 효과적으로 가져오는 방식으로 작성되었습니다. 또한, 쿼리 문법이 올바르고, 읽기 쉽게 작성되어 있습니다.

5. **개선 사항**: 이 쿼리는 이미 효율적으로 작성되어 있지만, OUTER JOIN을 사용하는 대신 INNER JOIN과 LEFT JOIN을 사용하여 쿼리를 분리할 수 있습니다. 또한, 인덱스를 생성하여 쿼리 성능을 개선할 수 있습니다. 예를 들어, 사용자 ID와 라이드 테이블의 사용자 ID에 인덱스를 생성하면 JOIN 연산의 성능을 개선할 수 있습니다. 또한, 쿼리에서 사용하는 칼럼만 선택하여 데이터를 가져오는 방식으로 최적화할 수 있습니다.
