# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 5:25:33 |
| Language | oraclesql |
| Runtime | 420 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/top-travellers/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - 이 쿼리는 사용자와 라이드 테이블을 조인하고, 그룹화하고, 정렬하기 때문에 시간 복잡도는 O(n log n)입니다. 여기서 n은 사용자와 라이드 테이블의 행 수를 의미합니다.

2. **공간 복잡도**: O(n) - 쿼리가 생성하는 임시 결과 집합의 크기는 사용자와 라이드 테이블의 행 수에 비례하므로 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 쿼리는 사용자와 라이드 테이블을 조인하고, 그룹화하고, 정렬하는 전통적인 관계형 데이터베이스 접근법을 사용합니다. Outer Join을 사용하여 사용자 테이블의 모든 행을 포함하고, NVL 함수를 사용하여 NULL 값을 0으로 대체합니다.

4. **잘된 점**: 이 쿼리는 문제를 명확하게 이해하고, 필요한 데이터를 효과적으로 조회하는 방법을 사용했습니다. Outer Join과 NVL 함수를 사용하여 NULL 값을 처리하는 방법이 좋습니다.

5. **개선 사항**: 이 쿼리는 이미 최적화된 쿼리이지만, 인덱스를 생성하여 조회 성능을 향상시킬 수 있습니다. 예를 들어, 사용자 테이블의 id 열과 라이드 테이블의 user_id 열에 인덱스를 생성하면 조인 성능을 향상시킬 수 있습니다. 또한, 쿼리에서 필요한 열만 조회하여 불필요한 데이터 전송을 줄일 수 있습니다.
