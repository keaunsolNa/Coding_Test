/* Write your PL/SQL query statement below */
SELECT T1.name
     , SUM(NVL(T2.distance, 0)) AS travelled_distance 
  FROM USERS T1
     , RIDES T2
 WHERE T1.id = T2.user_id(+)
 GROUP BY T1.name
 ORDER BY travelled_distance DESC, name