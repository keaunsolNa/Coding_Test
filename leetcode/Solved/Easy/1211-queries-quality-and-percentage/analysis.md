# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 21. 오후 5:32:02 |
| Language | oraclesql |
| Runtime | 265 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/queries-quality-and-percentage/submissions/)

## Code Review

코드 리뷰를 진행하겠습니다.

1. **시간 복잡도**: O(n^2) - 서브쿼리 내에서 전체 테이블을 스캔하는 작업이 반복되기 때문에 시간 복잡도가 높습니다. 특히, 서브쿼리에서 `COUNT(A.query_name)`를 사용하여 개별 쿼리 이름별로 데이터를 집계하는 작업이 반복되므로, 전체 데이터의 크기에 따라 성능이 저하될 수 있습니다.

2. **공간 복잡도**: O(n) - 쿼리 결과를 저장하기 위한 메모리 공간이 필요합니다. 특히, 서브쿼리에서 집계된 데이터를 저장하는 데 필요한 공간이 추가로 필요하므로, 공간 복잡도는 선형적으로 증가합니다.

3. **풀이 접근법**: 이 코드에서는 서브쿼리와 집계 함수를 사용하여 데이터를 처리합니다. 특히, `NVL` 함수와 `ROUND` 함수를 사용하여 결과를 가공하고, `GROUP BY` 절을 사용하여 쿼리 이름별로 데이터를 집계합니다. 또한, 서브쿼리에서 `COUNT` 함수를 사용하여 개별 쿼리 이름별로 데이터를 집계하는 작업을 수행합니다.

4. **잘된 점**: 코드는 문제의 요구 사항을 충족하며, 결과를 가공하는 부분에서 `NVL` 함수와 `ROUND` 함수를 사용하여 결과를 정리했습니다. 또한, `GROUP BY` 절을 사용하여 쿼리 이름별로 데이터를 집계하는 작업을 수행했습니다.

5. **개선 사항**: 서브쿼리에서 전체 테이블을 스캔하는 작업을 피할 수 있는 방법을 고려할 수 있습니다. 예를 들어, JOIN을 사용하여 데이터를 결합하거나, 인덱스를 생성하여 데이터를 효율적으로 조회할 수 있습니다. 또한, 집계 함수를 사용하는 부분에서 성능을 최적화할 수 있는 방법을 고려할 수 있습니다. 예를 들어, `COUNT` 함수 대신 `SUM` 함수를 사용하여 데이터를 집계할 수 있습니다. 또한, 서브쿼리에서 데이터를 집계하는 작업을 피할 수 있는 방법을 고려할 수 있습니다. 예를 들어, 윈도우 함수를 사용하여 데이터를 집계할 수 있습니다.
