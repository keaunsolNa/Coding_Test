# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오전 9:36:43 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/day-of-the-year/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(1) - 주어진 날짜 문자열을 파싱하고, 윤년 여부를 확인하며, 월별 일수를 더하는 과정이 모두 상수 시간에 수행되기 때문에 시간 복잡도는 O(1)입니다.

2. **공간 복잡도**: O(1) - 코드에서 사용하는 변수와 배열의 크기는 입력 크기에 따라 변하지 않기 때문에 공간 복잡도는 O(1)입니다.

3. **풀이 접근법**: 이 문제는 단순한 수학 계산과 문자열 파싱을 이용한 알고리즘으로 해결됩니다. 주어진 날짜 문자열을 년, 월, 일로 분리하고, 윤년 여부를 확인하여 2월의 일수를 결정합니다. 이후 월별 일수를 더하여 해당 날짜가一年 중 몇 번째 날인지 계산합니다.

4. **잘된 점**: 코드는 구조적으로 깔끔하고, 변수명이 명확하여 이해하기 쉽습니다. 또한, 윤년 여부를 확인하는 로직이 정확하게 구현되어 있습니다.

5. **개선 사항**: 코드는 이미 상당히 효율적이고 간결합니다. 그러나, 더 최적화하기 위해 날짜 문자열을 파싱하는 부분에서 `LocalDate` 클래스를 사용할 수 있습니다. Java 8 이상에서 제공하는 `LocalDate` 클래스를 사용하면 날짜 문자열을 더 쉽고 효율적으로 파싱할 수 있습니다. 예를 들어, `LocalDate.parse(date)`를 사용하여 날짜를 파싱할 수 있습니다. 하지만, 이 문제는 LeetCode에서 출제된 문제이므로, Java 8 이상의 기능을 사용할 수 있는지 확인해야 합니다.
