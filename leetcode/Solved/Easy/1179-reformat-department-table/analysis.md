# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 16. 오전 10:25:20 |
| Language | mysql |
| Runtime | 522 ms (Beats 71.1%) |
| Memory | 0B (Beats 100.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/reformat-department-table/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n) - 이 쿼리는 Department 테이블의 모든 행을 스캔하므로 시간 복잡도는 데이터의 크기(n)에 비례합니다. 각 행은 한 번씩 처리되므로 선형 시간 복잡도를 가지게 됩니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 각 id별로 그룹화되며, 각 그룹에 대해 12개의 월별 매출을 계산합니다. 따라서 결과 집합의 크기는 입력 데이터의 크기(n)에 비례하므로 공간 복잡도도 선형입니다.

3. **풀이 접근법**: 이 쿼리는 SQL의 집계 함수(SUM)와 조건부 연산(CASE WHEN)을 사용하여 데이터를 피벗하는 방식으로 문제를 해결합니다. 이는 데이터를 가로로 펼치기 위한 전형적인 패턴입니다. 각 월에 대한 매출을 계산하고, 이를 id별로 그룹화하여 결과를 도출합니다.

4. **잘된 점**: 코드는 문제를 명확하게 이해하고, 필요한 데이터를 효율적으로 추출하는 방식으로 작성되었습니다. SQL 쿼리는 간결하고, 각 월에 대한 매출을 계산하는 로직이 일관적입니다. 또한, 쿼리는 표준 SQL 구문을 사용하므로 다양한 데이터베이스 시스템에서 실행할 수 있습니다.

5. **개선 사항**: 현재 쿼리는 모든 월에 대해 별도의 열을 생성합니다. 만약 월이 동적으로 변경된다면(예: 새로운 월이 추가되는 경우), 쿼리를 동적으로 생성하거나, 데이터를 피벗하는 다른 방법을 고려해야 할 수 있습니다. 그러나 현재의 문제 정의에서는 이러한 동적 변화가 없으므로, 현재의 쿼리는 충분히 효율적입니다. 또한, 데이터베이스의 인덱스 설정이나 쿼리 최적화와 같은 추가적인 최적화 방법을 고려할 수 있지만, 이는 데이터의 크기와 사용 패턴에 따라 결정되어야 합니다.
