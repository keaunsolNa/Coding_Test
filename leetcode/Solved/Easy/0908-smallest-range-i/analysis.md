# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 12. 오전 9:25:54 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 42.5 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/smallest-range-i/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - Arrays.sort() 함수를 사용하여 배열을 정렬하는 데 O(n log n)의 시간이 소요됩니다. 이후 최소값과 최대값을 찾는 데는 O(1)의 시간이 소요됩니다.

2. **공간 복잡도**: O(1) - 입력 배열을 제외하고 추가적인 공간을 사용하지 않으므로 공간 복잡도는 O(1)입니다.

3. **풀이 접근법**: 문제를 해결하기 위해 정렬 알고리즘을 사용했습니다. 배열을 정렬한 후, 최소값과 최대값을 찾은 다음, 최대값과 최소값의 차이를 계산하여 범위를 결정했습니다. k 값을 고려하여 범위를 조정했습니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. Arrays.sort() 함수를 사용하여 배열을 정렬하는 부분은 효율적입니다. 또한, Math.min()과 Math.max() 함수를 사용하여 범위를 계산하는 부분도 잘 구현되어 있습니다.

5. **개선 사항**: 현재 코드는 이미 효율적이지만, 입력 배열이 이미 정렬되어 있는 경우에는 더 효율적인 알고리즘을 사용할 수 있습니다. 예를 들어, 두 개의 포인터를 사용하여 최소값과 최대값을 찾을 수 있습니다. 그러나 입력 배열이 무작위로 정렬되어 있는 경우에는 현재의 정렬 기반 접근법이 가장 효율적인 방법입니다.
