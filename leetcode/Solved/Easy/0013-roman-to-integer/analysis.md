# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 12. 오전 11:08:42 |
| Language | java |
| Runtime | 2 ms (Beats 0.0%) |
| Memory | 43 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/roman-to-integer/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하는 시간 복잡도를 가지고 있습니다. 이는 문자열을 한 번만 순회하기 때문입니다.

2. **공간 복잡도**: O(1) - 입력 문자열을 배열로 변환하는 것을 제외하면, 입력 크기에 따라 공간 사용량이 증가하지 않습니다. 따라서 상수 공간 복잡도를 가지고 있습니다.

3. **풀이 접근법**: 이 문제는 문자열을 순회하면서 이전 문자와 현재 문자를 비교하여 총합을 계산하는 방식으로 해결되었습니다. 이는 그리디 알고리즘의 특징을 가지고 있습니다. 로마 숫자의 특성상, 작은 숫자가 큰 숫자의 왼쪽에 오면 작은 숫자를 뺀 값을 더하는 방식으로 계산됩니다.

4. **잘된 점**: 코드는 간결하고, 로마 숫자를 정수로 변환하는 로직이 명확하게 구현되어 있습니다. 또한, `convertCharToInt` 메서드를 분리하여 코드의 가독성을 높였습니다.

5. **개선 사항**: 코드는 이미 충분히 최적화되어 있습니다. 그러나, `System.out.println` 문은 디버깅 용도로 사용되는 것으로 보이므로, 실제 제출 시에는 제거하는 것이 좋습니다. 또한, 변수 이름을 더 의미 있게 변경하여 코드의 가독성을 높일 수 있습니다. 예를 들어, `prev`를 `previousValue`로, `now`를 `currentValue`로 변경할 수 있습니다.
