# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 12. 오전 11:08:10 |
| Language | java |
| Runtime | 2 ms (Beats 0.0%) |
| Memory | 42.7 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/roman-to-integer/submissions/)

## Code Review

로마 숫자를 정수로 변환하는 문제의 코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하여 단 한번의 반복문을 수행하기 때문에 선형 시간 복잡도를 가집니다. 이는 매우 효율적인 시간 복잡도입니다.

2. **공간 복잡도**: O(n) - 입력 문자열을 문자 배열로 변환하여 저장하기 때문에 공간 복잡도도 입력 문자열의 길이(n)에 비례합니다. 그러나 실제로 사용하는 추가 공간은 상수 크기이므로 O(1)로 간주할 수도 있습니다.

3. **풀이 접근법**: 이 코드는 단순한 반복과 비교를 이용한 알고리즘을 사용합니다. 로마 숫자의 특성을 이용하여, 현재 숫자가 이전 숫자보다 큰 경우에는 이전 숫자를 뺀 값을 더하고, 그렇지 않은 경우에는 현재 숫자를 더하는 방식으로 총합을 계산합니다.

4. **잘된 점**: 코드는 매우 간결하고 이해하기 쉽습니다. 로마 숫자를 정수로 변환하는 함수를 별도로 구현하여 코드의 가독성을 높였습니다. 또한, 변수명이 명확하여 코드의 의도를 쉽게 파악할 수 있습니다.

5. **개선 사항**: 코드는 이미 매우 효율적이고 간결합니다. 그러나, 입력 문자열의 유효성을 검사하는 코드를 추가하여 예외 상황을 처리할 수 있습니다. 또한, `System.out.println`문을 제거하여 코드를 더 깨끗하게 유지할 수 있습니다. 마지막으로, 자바 8 이상의 버전에서는 `switch`문 대신에 `Map`을 이용하여 로마 숫자를 정수로 변환할 수 있습니다. 이는 코드를 더 함수형 프로그래밍에 가깝게 만들 수 있습니다.
