# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 20. 오후 4:30:20 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 43.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/shift-2d-grid/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(m*n) - 2차원 그리드의 모든 요소를 한 번씩 방문하여 처리하기 때문에, 시간 복잡도는 그리드의 크기(m*n)에 비례합니다. 여기서 m은 행의 수, n은 열의 수입니다.

2. **공간 복잡도**: O(m*n) - 원본 그리드의 모든 요소를 1차원 배열에 복사하고, 이동한 배열을 추가로 생성하기 때문에, 공간 복잡도도 그리드의 크기(m*n)에 비례합니다.

3. **풀이 접근법**: 이 코드는 2차원 그리드의 모든 요소를 1차원 배열로 변환하고, 이 배열을 지정된 수(k)만큼 회전시킵니다. 회전된 배열을 다시 2차원 그리드로 변환하여 결과를 얻습니다. 이는 배열 회전 문제를 해결하는 일반적인 접근법입니다.

4. **잘된 점**: 코드는 문제를 명확하게 이해하고, 2차원 그리드를 1차원 배열로 변환하여 회전시키는 아이디어를 제대로 구현했습니다. 또한, 결과를 리스트로 반환하는 부분도 잘 처리했습니다.

5. **개선 사항**: 코드에서 일부 불필요한 부분이 있습니다. 예를 들어, `xL` 변수를 사용하여 내부 리스트에 요소를 추가할 때, `(xL + j) % length`를 사용하는 대신, 단순히 `i * y + j`를 사용하여 인덱스를 계산할 수 있습니다. 또한, `System.out.print`문은 디버깅 목적으로 사용하는 것으로 보이는데, 실제 제출 코드에서는 이러한 부분을 제거하는 것이 좋습니다. 마지막으로, 변수 이름을 더 의미 있게 지어 코드의 가독성을 높일 수 있습니다.
