# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 23. 오전 11:03:34 |
| Language | mysql |
| Runtime | 167 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-books-with-no-available-copies/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n log n) - 이 쿼리는 서브쿼리와 조인을 사용하여 데이터를 처리합니다. 서브쿼리에서 데이터를 그룹화하고, 조인에서 데이터를 결합하는 과정에서 시간 복잡도가 발생합니다. 특히, ORDER BY 절에서 데이터를 정렬하는 과정에서 n log n의 시간 복잡도가 발생합니다.

2. **공간 복잡도**: O(n) - 이 쿼리는 임시 결과 집합을 생성하여 처리합니다. 특히, WITH 절에서 생성된 임시 테이블 W는 최대 n개의 행을 가질 수 있습니다. 따라서 공간 복잡도는 O(n)입니다.

3. **풀이 접근법**: 이 쿼리는 다음 알고리즘과 패턴을 사용합니다.
 * WITH 절을 사용하여 임시 결과 집합을 생성합니다.
 * GROUP BY 절을 사용하여 데이터를 그룹화합니다.
 * JOIN 절을 사용하여 데이터를 결합합니다.
 * ORDER BY 절을 사용하여 데이터를 정렬합니다.
 * 서브쿼리를 사용하여 데이터를 필터링합니다.

4. **잘된 점**: 이 쿼리는 다음과 같은 점에서 잘 구현되어 있습니다.
 * WITH 절을 사용하여 임시 결과 집합을 생성하여 코드를 간결하게 만들었습니다.
 * GROUP BY 절을 사용하여 데이터를 그룹화하여 필요한 데이터를 효율적으로 추출했습니다.
 * JOIN 절을 사용하여 데이터를 결합하여 필요한 데이터를 결합했습니다.

5. **개선 사항**: 이 쿼리는 다음과 같은 점에서 개선할 수 있습니다.
 * INDEX를 생성하여 쿼리 성능을 개선할 수 있습니다. 특히, book_id 열에 INDEX를 생성하면 JOIN과 WHERE 절의 성능을 개선할 수 있습니다.
 * 서브쿼리를 사용하여 데이터를 필터링하는 대신, JOIN과 WHERE 절을 사용하여 데이터를 필터링할 수 있습니다.
 * ORDER BY 절에서 데이터를 정렬하는 대신, LIMIT 절을 사용하여 필요한 데이터만 추출할 수 있습니다.
