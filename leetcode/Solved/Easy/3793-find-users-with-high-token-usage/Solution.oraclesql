/* Write your PL/SQL query statement below */
SELECT T1.user_id
     , COUNT(T1.prompt) AS prompt_count
     , ROUND(AVG(T1.tokens), 2) AS avg_tokens
  FROM prompts T1
 GROUP BY user_id  
HAVING COUNT(T1.prompt) >= 3
ORDER BY T1.user_id