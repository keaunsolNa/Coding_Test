# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 13. 오후 4:45:50 |
| Language | oraclesql |
| Runtime | 374 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-users-with-high-token-usage/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 prompts 테이블의 모든 행을 스캔하여 사용자별 프롬프트 수와 평균 토큰 수를 계산하므로, 시간 복잡도는 데이터의 크기(n)에 비례합니다.

2. **공간 복잡도**: O(n) - 쿼리 결과를 저장하기 위해 필요한 공간은 사용자 수에 비례하므로, 공간 복잡도도 데이터의 크기(n)에 비례합니다.

3. **풀이 접근법**: 이 쿼리는 집계 함수(COUNT, AVG)를 사용하여 사용자별 프롬프트 수와 평균 토큰 수를 계산하고, HAVING 절을 사용하여 프롬프트 수가 3 이상인 사용자만 필터링합니다. 이는 간단한 집계 및 필터링 알고리즘을 사용한 접근법입니다.

4. **잘된 점**: 쿼리는 간결하고 읽기 쉽습니다. 또한, 필요한 열만 선택하여 불필요한 데이터 전송을 피하고 있습니다.

5. **개선 사항**: 쿼리는 이미 최적화되어 있지만, 인덱스를 생성하여 쿼리 성능을 개선할 수 있습니다. 예를 들어, user_id 열에 인덱스를 생성하면 GROUP BY 절의 성능을 개선할 수 있습니다. 또한, 토큰 수를 계산하는 열이 존재하지 않는다면, 토큰 수를 계산하는 함수를 생성하여 쿼리를 더 간결하게 만들 수 있습니다.
