# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 13. 오후 4:51:59 |
| Language | oraclesql |
| Runtime | 438 ms (Beats 29.7%) |
| Memory | 0B (Beats 100.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-users-with-high-token-usage/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: 이 쿼리의 시간 복잡도는 O(n log n)입니다. 이는 데이터를 그룹화하고 정렬하는 데 필요한 시간 복잡도 때문입니다. 특히, GROUP BY와 ORDER BY 연산이 전체 데이터를 스캔하고 정렬해야 하므로 시간 복잡도가 증가합니다.

2. **공간 복잡도**: 이 쿼리의 공간 복잡도는 O(n)입니다. 이는 쿼리 결과를 저장하기 위해 필요한 메모리 공간 때문입니다. 특히, GROUP BY와 ORDER BY 연산이 임시 결과를 저장해야 하므로 공간 복잡도가 증가합니다.

3. **풀이 접근법**: 이 쿼리는 집계 함수와 그룹화 연산을 사용하여 사용자별 프롬프트 수와 평균 토큰 수를 계산합니다. 특히, COUNT, AVG, MIN, MAX 함수를 사용하여 필요한 통계량을 계산하고, HAVING 절을 사용하여 조건을 필터링합니다.

4. **잘된 점**: 이 쿼리는 필요한 열만 선택하고, 적절한 집계 함수와 그룹화 연산을 사용하여 효율적으로 데이터를 처리합니다. 또한, ROUND 함수를 사용하여 평균 토큰 수를 소수점 2자리까지 반올림하여 결과를 더 읽기 쉽게 합니다.

5. **개선 사항**: 이 쿼리는 이미 비교적 효율적으로 작성되어 있지만, 일부 최적화가 가능합니다. 예를 들어, 인덱스를 생성하여 GROUP BY와 ORDER BY 연산의 성능을 개선할 수 있습니다. 또한, 쿼리 결과를 캐시하여 동일한 쿼리가 반복적으로 실행될 때 성능을 개선할 수 있습니다. 그러나 이러한 최적화는 데이터베이스의 특정 설정과 사용 패턴에 따라 달라질 수 있습니다.
