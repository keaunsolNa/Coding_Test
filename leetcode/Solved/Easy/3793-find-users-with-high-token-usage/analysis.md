# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 13. 오후 4:50:50 |
| Language | oraclesql |
| Runtime | 217 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-users-with-high-token-usage/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n) - 이 쿼리는 사용자별로 프롬프트를 그룹화하고, 각 그룹에서 프롬프트의 개수를 세고, 평균 토큰 수를 계산합니다. 또한, MIN과 MAX 함수를 사용하여 토큰 수의 범위를 확인합니다. 이러한 연산들은 데이터의 크기(n)에 비례하며, ORDER BY 절에서 데이터를 정렬해야 하므로 log n의 시간 복잡도가 추가됩니다.

2. **공간 복잡도**: O(n) - 쿼리는 사용자별로 그룹화된 데이터를 임시로 저장해야 하므로, 공간 복잡도는 데이터의 크기(n)에 비례합니다.

3. **풀이 접근법**: 이 쿼리는 집계 함수(AVG, COUNT, MIN, MAX)와 GROUP BY, HAVING 절을 사용하여 사용자별로 프롬프트의 개수와 평균 토큰 수를 계산합니다. 또한, ORDER BY 절을 사용하여 평균 토큰 수가 높은 사용자를 먼저 출력합니다. 이는 관계형 데이터베이스에서 데이터를 분석하고 집계하는 일반적인 패턴입니다.

4. **잘된 점**: 코드는 명확하고 간결하며, 필요한 연산들을 효율적으로 수행합니다. 특히, ROUND 함수를 사용하여 평균 토큰 수를 2자리까지 반올림하여 출력하는 부분이 좋습니다.

5. **개선 사항**: 쿼리 자체는 효율적으로 작성되어 있으나, 데이터의 크기가 매우 클 경우, 인덱스를 생성하여 쿼리 성능을 개선할 수 있습니다. 또한, 사용자별로 프롬프트의 개수와 평균 토큰 수를 계산하는 부분을 서브쿼리 또는 조인으로 분리하여 쿼리를 더 읽기 쉽게 만들 수 있습니다. 그러나, 이러한 개선 사항은 데이터의 크기와 쿼리 성능에 따라 결정되어야 합니다.
