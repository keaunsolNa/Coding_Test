# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오후 3:44:15 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 41.8 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/subtract-the-product-and-sum-of-digits-of-an-integer/submissions/)

## Code Review

코드 리뷰를 진행하겠습니다.

1. **시간 복잡도**: O(log n) - 입력 숫자 n의 자릿수에 비례하는 시간 복잡도를 가지고 있습니다. 이는 숫자를 문자열로 변환하여 각 자릿수를 반복적으로 처리하기 때문입니다.

2. **공간 복잡도**: O(log n) - 입력 숫자 n의 자릿수에 비례하는 공간 복잡도를 가지고 있습니다. 이는 문자열로 변환된 숫자를 저장하기 위한 공간이 필요하기 때문입니다.

3. **풀이 접근법**: 본 코드는 간단한 반복문을 사용하여 숫자의 각 자릿수를 처리합니다. 문자열로 변환된 숫자의 각 자릿수를 반복적으로 처리하여 곱과 합을 계산합니다. 이는 기본적인 반복 알고리즘을 사용한 접근법입니다.

4. **잘된 점**: 코드는 간결하고 이해하기 쉽습니다. 변수의 이름이 명확하여 코드의 의도를 쉽게 알 수 있습니다. 또한, 문제의 요구 사항을 정확하게 구현했습니다.

5. **개선 사항**: 본 코드는 이미 충분히 간결하고 효율적입니다. 그러나, 숫자의 자릿수를 계산하는 부분에서 Math.log10 함수를 사용하여 자릿수를 계산할 수 있습니다. 하지만, 본 코드에서 사용된 문자열로 변환하여 자릿수를 처리하는 방법도 충분히 효율적입니다. 또한, 자릿수의 곱과 합을 계산하는 부분에서 overflow가 발생할 수 있으므로, long 타입의 변수를 사용하여 overflow를 방지할 수 있습니다. 그러나, 문제의 제한 조건을 고려하면 본 코드는 충분히 적절합니다.
