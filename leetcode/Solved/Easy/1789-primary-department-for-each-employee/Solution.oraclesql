/* Write your PL/SQL query statement below */
SELECT T1.employee_id
     , CASE WHEN count(T1.department_id) = 1 THEN  1
            ELSE 2
            END AS department_id         
  FROM Employee T1
 GROUP BY T1.employee_id 
 ORDER BY T1.employee_id