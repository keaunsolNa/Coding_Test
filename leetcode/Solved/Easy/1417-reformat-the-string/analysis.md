# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 21. 오후 1:44:46 |
| Language | java |
| Runtime | 38 ms (Beats 7.1%) |
| Memory | 53.2 MB (Beats 5.7%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/reformat-the-string/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하는 시간 복잡도를 가지고 있습니다. 이는 문자열을 두 번의 replaceAll() 메서드를 통해 숫자와 알파벳으로 분리하고, 이후에 문자열을 조합하는 과정에서 발생합니다.

2. **공간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하는 공간 복잡도를 가지고 있습니다. 이는 숫자와 알파벳을 분리하여 저장하는 과정에서 발생하며, 결과 문자열을 저장하는 데에도 공간이 필요합니다.

3. **풀이 접근법**: 이 코드는 두 가지 문자열(숫자와 알파벳)을 분리하여 저장하고, 이후에 번갈아가며 조합하는 알고리즘을 사용합니다. 이는 문제의 요구 사항을 직접적으로 해결하는 접근법입니다.

4. **잘된 점**: 코드는 문제의 요구 사항을 명확하게 이해하고, 이를 간결하게 구현했습니다. 특히, 숫자와 알파벳을 분리하는 과정에서 정규식을 사용한 점은 코드를 간결하게 만들었습니다.

5. **개선 사항**: 코드는 이미 khá 효율적이지만, 일부 변수명과 주석을 추가하여 코드의 가독성을 높일 수 있습니다. 또한, 결과 문자열을 StringBuilder를 사용하여 생성하는 것이 문자열을 반복적으로 연결하는 것보다 효율적일 수 있습니다. 예를 들어, `answer`를 StringBuilder로 선언하고, `append()` 메서드를 사용하여 문자열을 조합할 수 있습니다.
