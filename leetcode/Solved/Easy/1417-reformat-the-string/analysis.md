# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 21. 오후 1:41:50 |
| Language | java |
| Runtime | 5 ms (Beats 0.0%) |
| Memory | 44.9 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/reformat-the-string/submissions/)

## Code Review

리뷰 대상 코드는 LeetCode의 "reformat-the-string" 문제를 해결하는 자바 솔루션입니다. 

1. **시간 복잡도**: O(n) - 여기서 n은 입력 문자열의 길이입니다. 이유는 문자열에서 숫자와 알파벳을 분리하는 작업이 두 번의 반복으로 수행되며, 이후 문자열을 재구성하는 작업도 한 번의 반복으로 수행되기 때문입니다.

2. **공간 복잡도**: O(n) - 입력 문자열의 길이에 비례하는 추가 공간이 필요합니다. 이유는 입력 문자열에서 숫자와 알파벳을 분리하여 별도의 문자열에 저장하기 때문입니다.

3. **풀이 접근법**: 이 솔루션은 두 가지 주요 단계로 구성됩니다. 첫째, 입력 문자열에서 숫자와 알파벳을 분리하여 별도의 문자열에 저장합니다. 둘째, 분리된 숫자와 알파벳 문자열을 교대로 결합하여 결과 문자열을 생성합니다. 이는 기본적인 문자열 조작과 반복을 이용한 접근법입니다.

4. **잘된 점**: 코드는 문제를 명확하게 이해하고, 간단한 문자열 조작을 통해 문제를 해결했습니다. 또한, 코드의 구조가 비교적 간결하고 이해하기 쉽습니다.

5. **개선 사항**: 현재 코드에서 `alp` 문자열을 생성할 때, 숫자가 아닌 문자를 추출해야 하지만, 현재 코드는 숫자가 아닌 문자를 추출하는 대신에 숫자를 추출하는 정규식 `[0-9]`을 사용하고 있습니다. 따라서, `alp` 문자열을 생성할 때는 숫자가 아닌 문자를 추출하는 정규식 `[^0-9]`을 사용해야 합니다. 또한, `number`와 `alp`를 생성할 때, 각각의 정규식이 올바르게 사용되고 있는지 확인해야 합니다. 현재 코드에서는 `alp`를 생성할 때도 숫자를 추출하는 정규식을 사용하고 있으므로, 이는 올바르지 않습니다. 

```java
String number = s.replaceAll("[^0-9]", "");
String alp = s.replaceAll("[0-9]", "");
```
