# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 21. 오전 11:12:32 |
| Language | java |
| Runtime | 1 ms (Beats 92.3%) |
| Memory | 43.3 MB (Beats 87.4%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/excel-sheet-column-number/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n)입니다. 여기서 n은 입력 문자열 `columnTitle`의 길이입니다. 이는 문자열의 각 문자를 한 번씩만 처리하기 때문입니다.

2. **공간 복잡도**: O(1)입니다. 입력 문자열을 제외하고는 추가적인 공간을 사용하지 않기 때문입니다. 변수 `ans`, `tar`, `val`, `i`는 상수 공간을 차지합니다.

3. **풀이 접근법**: 이 문제는 26진법을 10진법으로 변환하는 문제로 볼 수 있습니다. 각 문자를 해당하는 숫자로 변환하고, 이를 26의 제곱으로 곱한 값을 누적하여 결과를 얻습니다. 이는 기본적인 수制 변환 알고리즘을 사용한 것입니다.

4. **잘된 점**: 코드는 매우 간결하고 이해하기 쉽습니다. 변수 이름이 의미를 잘 전달하고, 로직이 단순하여 버그를 찾기 쉽습니다. 또한, 문자를 숫자로 변환하는 부분에서 `'A'`를 사용하여 ASCII 코드의 차이를 이용한 점이 좋습니다.

5. **개선 사항**: 코드는 이미 충분히 최적화되어 있습니다. 하지만, 에러 처리를 추가할 수 있습니다. 예를 들어, 입력 문자열이 null이거나 빈 문자열인 경우를 처리할 수 있습니다. 또한, 입력 문자열에 유효하지 않은 문자가 포함된 경우를 처리할 수 있습니다. 이러한 추가적인 처리는 코드의 완전성을 높일 수 있습니다.
