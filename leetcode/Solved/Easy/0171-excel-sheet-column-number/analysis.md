# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 21. 오전 10:28:20 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 42.6 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/excel-sheet-column-number/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하여 반복문이 실행되므로 선형 시간 복잡도를 가지며, 이는 매우 효율적입니다. 문자열의 각 문자를 단 한번씩 처리하므로 최적의 시간 복잡도를 달성하고 있습니다.

2. **공간 복잡도**: O(n) - 입력 문자열을 문자 배열로 변환하여 저장하므로, 공간 복잡도 또한 입력 문자열의 길이에 비례합니다. 그러나 이는 문제를 해결하기 위해 필요한 최소한의 공간이므로, 공간 사용에 있어 효율적이라고 할 수 있습니다.

3. **풀이 접근법**: 이 코드는 Excel 컬럼 제목을 숫자로 변환하는 문제를 해결하기 위해, 문자열을 뒤에서부터 앞으로 처리하며 각 문자의 값을 계산하여 총합을 구하는 방식을 사용합니다. 이는 26진법을 사용하여 숫자를 표현하는 Excel 컬럼 제목의 특성을 잘 활용하고 있습니다.

4. **잘된 점**: 코드는 문제를 명확하게 이해하고, 해당 문제를 해결하기 위한 논리를 간결하게 구현했습니다. 또한, 각 문자의 값을 계산하는 부분에서 26진법의 특성을 잘 반영하여, 코드의 논리가 명확하고 쉽게 이해할 수 있습니다.

5. **개선 사항**: 코드 내에 있는 디버깅용 println 문들은 제거하는 것이 좋습니다. 또한, 변수명이 조금 더 명확해졌으면 좋겠으며, 예를 들어 `arr` 대신 `chars`로, `ans` 대신 `result`로, `target` 대신 `charValue`로 변경하여 코드의 가독성을 높일 수 있습니다. 마지막으로, idx를 계산하여 26의 거듭제곱을 더하는 부분은 Math.pow를 사용하거나, 별도의 변수를 두어 26의 거듭제곱을 계산하여 사용하면, 코드의 효율성과 가독성이 더提高될 수 있습니다.
