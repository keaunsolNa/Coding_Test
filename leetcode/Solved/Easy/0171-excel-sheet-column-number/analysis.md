# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 21. 오전 10:29:33 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 42.5 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/excel-sheet-column-number/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 입력 문자열의 길이(n)에 비례하는 시간 복잡도를 가지고 있습니다. 이는 문자열을 한 번만 순회하기 때문입니다.

2. **공간 복잡도**: O(n) - 입력 문자열을 문자 배열로 변환하기 때문에 공간 복잡도도 문자열의 길이(n)에 비례합니다.

3. **풀이 접근법**: 이 문제는 26진법을 10진법으로 변환하는 문제입니다. 사용된 알고리즘은 각 자릿수에 해당하는 값을 계산하여 총합을 구하는 것입니다. 이는 10진법에서 26진법으로 변환하는 방식과 유사합니다.

4. **잘된 점**: 코드는 문제를 잘 이해하고, 각 자릿수에 해당하는 값을 계산하여 총합을 구하는 로직을 구현했습니다. 또한, 문자를 숫자로 변환하는 부분에서 'A'를 빼는 방식으로 아스키 코드 값을 활용하여 계산했습니다.

5. **개선 사항**: 코드에서 계산 부분을 간소화할 수 있습니다. 현재 코드에서는 idx가 0일 때와 아닐 때를 분리하여 계산하고 있습니다. 하지만, 26의 제곱을 계산하여 곱하는 방식으로도 구현할 수 있습니다. 예를 들어, `ans += (arr[i] - 'A' + 1) * Math.pow(26, arr.length - i - 1)`와 같이 구현할 수 있습니다. 또한, 디버깅을 위한 System.out.println 문을 제거하여 코드를 깔끔하게 유지할 수 있습니다.
