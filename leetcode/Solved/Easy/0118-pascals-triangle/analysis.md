# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 14. 오전 10:46:39 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 42.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/pascals-triangle/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: 이 코드의 시간 복잡도는 O(n^2)입니다. 파스칼의 삼각형을 생성하기 위해 2중 루프를 사용하기 때문입니다. 외부 루프는 `numRows`만큼 반복하고, 내부 루프는 이전 행의 원소 개수만큼 반복합니다.

2. **공간 복잡도**: 이 코드의 공간 복잡도는 O(n^2)입니다. 파스칼의 삼각형을 저장하기 위해 2차원 리스트를 사용하기 때문입니다. 각 행의 원소 개수는 1부터 `numRows`까지 증가합니다.

3. **풀이 접근법**: 이 코드는 동적 프로그래밍 알고리즘을 사용하여 파스칼의 삼각형을 생성합니다. 이전 행의 원소 값을 사용하여 다음 행의 원소 값을 계산합니다. 이는 파스칼의 삼각형의 특성인 각 원소가 두 개 위의 원소의 합으로 계산되는 것을 이용합니다.

4. **잘된 점**: 코드는 파스칼의 삼각형을 생성하는 로직을 명확하게 구현했습니다. 또한, 각 행의 원소 개수를 효율적으로 계산하여 메모리 사용을 최적화했습니다.

5. **개선 사항**: 코드에서 개선할 수 있는 부분은 변수 이름을 더 명확하게 지정하는 것입니다. 예를 들어, `first`와 `second` 변수는 `firstRow`와 `secondRow`로 이름을 변경할 수 있습니다. 또한, 코드의 가독성을 높이기 위해 더 많은 주석을 추가할 수 있습니다. 또한, `prev` 변수는 불필요한 변수로, `list.get(i - 1)`을 직접 사용할 수 있습니다.
