# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026-05-24 14:03:43 |
| Language | java |
| Runtime | 30 ms (Beats 92.8%) |
| Memory | 48.6 MB (Beats 48.5%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/distribute-candies/submissions/)

## Code Review

코드 리뷰를 다음과 같이 진행하겠습니다.

1. **시간 복잡도**: Big-O 표기법으로 O(n)으로 표현할 수 있습니다. 이는 코드에서 candyType 배열을 순회하여 Set에 추가하는 과정에서 발생합니다. Set에 추가하는 과정은 기본적으로 상수 시간복잡도인 O(1)이며, 이를 n번 반복하므로 전체 시간 복잡도는 O(n)입니다.

2. **공간 복잡도**: Big-O 표기법으로 O(n)으로 표현할 수 있습니다. 이는 코드에서 HashSet을 사용하여 candyType 배열의 요소를 중복되지 않게 저장하기 때문입니다. 최악의 경우, 모든 요소가 서로 다를 수 있으므로 HashSet의 크기는 n이 되며, 이는 O(n) 공간 복잡도를 의미합니다.

3. **풀이 접근법**: 사용된 알고리즘은 Set을 이용한 중복 제거입니다. 이는 제한된 수의 캔디를 먹을 수 있으며, 캔디의 종류를 최대한 다양하게 먹는 것이 목표이므로, Set을 사용하여 중복되는 캔디를 제거하여 종류의 수를 계산합니다. 이 접근법은 간단하면서도 효과적입니다.

4. **잘된 점**: 코드에서 잘 구현된 점은 변수명을 명확하게 사용하였고, 코드의 논리가 간단하고 쉽게 이해할 수 있다는 점입니다. 또한, Math.min 함수를 사용하여 canEat과 Set의 크기 중 작은 값을 반환하는 부분이 직관적입니다.

5. **개선 사항**: 현재 코드는 이미 간단하고 효율적인 풀이입니다. 다만, Set을 초기화할 때, candyType의 크기만큼의 초기 용량을 지정할 수 있습니다. 그러나 기본적으로 Java의 HashSet은 자동으로 크기가 조절되므로, 큰 차이는 없습니다. 다른 개선 요소는 없으며, 현재 구현이 문제를 잘 해결하고 있습니다.
