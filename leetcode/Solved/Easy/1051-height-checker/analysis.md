# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 2:04:50 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 42.8 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/height-checker/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n log n)입니다. Arrays.sort() 함수는 퀵소트 알고리즘을 사용하여 정렬을 수행하며, 이는 평균적으로 O(n log n)의 시간 복잡도를 가집니다. 이후 반복문을 통해 원본 배열과 정렬된 배열을 비교하여 결과를 계산하므로, 추가적인 O(n)의 시간 복잡도가 발생합니다. 그러나 전체 시간 복잡도는 여전히 O(n log n)으로 유지됩니다.

2. **공간 복잡도**: O(n)입니다. 원본 배열의 복사본을 생성하여 정렬을 수행하므로, 추가적인 O(n)의 공간 복잡도가 발생합니다.

3. **풀이 접근법**: 문제를 해결하기 위해 정렬 알고리즘을 사용했습니다. 원본 배열의 복사본을 생성하고, Arrays.sort() 함수를 사용하여 정렬을 수행했습니다. 이후 원본 배열과 정렬된 배열을 비교하여 결과를 계산했습니다. 이는 간단하고 직관적인 접근법입니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. Arrays.sort() 함수를 사용하여 정렬을 수행했으며, 반복문을 통해 결과를 계산했습니다. 변수명도 명확하여 코드의 의도를 쉽게 이해할 수 있습니다.

5. **개선 사항**: 현재의 코드는 충분히 효율적입니다. 그러나 더 최적화된 접근법을 사용하고 싶다면, 카운팅 정렬 알고리즘을 사용할 수 있습니다. 카운팅 정렬은 정렬을 수행하는 데에 O(n + k)의 시간 복잡도를 가집니다. 여기서 k는 배열의 최대값과 최소값의 차이입니다. 이는 문제에서 주어진 조건에 따라 더 효율적인 결과를 낼 수 있습니다. 그러나 현재의 코드는 이미 충분히 간결하고 효율적이므로, 추가적인 최적화는 필요하지 않을 수 있습니다.
