# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 3:46:25 |
| Language | oraclesql |
| Runtime | 228 ms (Beats 0.0%) |
| Memory | 0B (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/fix-names-in-a-table/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 Users 테이블의 모든 행을 스캔해야 하므로 시간 복잡도는 데이터의 크기(n)에 비례합니다. UPPER와 SUBSTR 함수는 상수 시간 내에 실행되므로 전체 쿼리의 시간 복잡도에 영향을 미치지 않습니다.

2. **공간 복잡도**: O(n) - 쿼리 결과는 원본 테이블의 행 수(n)만큼의 공간을 차지하므로 공간 복잡도도 O(n)입니다. 쿼리 결과를 저장하기 위한 임시 공간이 필요하기 때문입니다.

3. **풀이 접근법**: 이 쿼리는 문자열 조작 함수(UPPER, SUBSTR, LENGTH)를 사용하여 이름을 대문자와 소문자로 변환합니다. 이는 문자열을 처리하는 가장 기본적인 접근법입니다. 쿼리는 Users 테이블에서 이름을 수정하여 새로운 결과를 생성합니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. 필요한 열만 선택하고, 이름을 수정하는 로직이 명확합니다. 또한 ORDER BY 절을 사용하여 결과를 user_id로 정렬하여 결과를 더 이해하기 쉽게 만들었습니다.

5. **개선 사항**: 현재 쿼리는 이름을 수정하는 로직이 단순하여 추가적인 최적화는 필요하지 않습니다. 그러나 대문자와 소문자로 이름을 변환하는 로직이 더 복잡해질 경우, 함수를 생성하여 코드를 더 모듈화할 수 있습니다. 또한, 이름의 길이가 매우 길 경우, SUBSTR 함수의 성능이 저하될 수 있으므로, 이 경우에는 다른 문자열 처리 함수를 사용하는 것이 더 효율적일 수 있습니다.
