# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 14. 오후 3:46:45 |
| Language | oraclesql |
| Runtime | 740 ms (Beats 14.8%) |
| Memory | 0B (Beats 100.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/fix-names-in-a-table/submissions/)

## Code Review

코드 리뷰를 다음과 같이 진행하겠습니다.

1. **시간 복잡도**: O(n) - 이 쿼리는 Users 테이블의 모든 행을 스캔하므로 시간 복잡도는 행의 수에 비례합니다. 즉, 테이블의 크기가 n일 때, 쿼리 수행 시간은 n에 비례합니다.

2. **공간 복잡도**: O(n) - 쿼리 결과도 Users 테이블의 행 수에 비례하므로 공간 복잡도 또한 O(n)입니다. 이는 결과 집합의 크기가 입력 테이블의 크기와 비슷하기 때문입니다.

3. **풀이 접근법**: 이 쿼리는 문자열 조작 함수를 사용하여 이름을 대문자와 소문자로 변환합니다. UPPER와 LOWER 함수를 사용하여 이름의 첫 글자는 대문자로, 나머지는 소문자로 변환합니다. SUBSTR 함수를 사용하여 이름의 첫 글자와 나머지 부분을 분리합니다.

4. **잘된 점**: 코드는 간결하고 읽기 쉽습니다. 필요한 문자열 조작 함수를 적절히 사용하여 이름을 변환했습니다. 또한, 쿼리 결과를 user_id 기준으로 정렬하여 결과를 구조적으로 출력했습니다.

5. **개선 사항**: 현재 쿼리는 Oracle SQL을 사용하고 있으므로, 다른 데이터베이스 시스템에서 사용할 경우 함수 이름이나 사용법이 다를 수 있습니다. 예를 들어, MySQL에서는 CONCAT 함수를 사용하여 문자열을 연결할 수 있습니다. 또한, 이름의 길이가 1인 경우를 고려하여 예외 처리를 추가할 수 있습니다. 하지만, 현재 쿼리는 간결하고 효율적이므로 큰 개선 사항은 없습니다.
