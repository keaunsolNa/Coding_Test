# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오전 9:17:38 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 45.4 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-common-characters/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(N \* M) - 여기서 N은 단어의 개수, M은 단어의 평균 길이입니다. 각 단어를 한 번씩 순회하여 문자의 빈도수를 계산하고, 최종적으로 공통 문자를 찾기 위해 배열을 순회하기 때문입니다.

2. **공간 복잡도**: O(N \* M) - 각 단어의 문자 빈도수를 저장하기 위한 해시맵과, 공통 문자를 저장하기 위한 리스트가 필요합니다. 최악의 경우, 모든 단어가 동일한 문자로 구성되어 있을 수 있으므로 공간 복잡도는 입력 크기에 비례합니다.

3. **풀이 접근법**: 이 풀이는 해시맵을 사용하여 각 단어의 문자 빈도수를 계산하고, 공통 문자를 찾기 위해 배열을 사용합니다. 각 단어의 문자 빈도수를 계산한 후, 공통 문자의 최소 빈도수를 찾습니다. 이는 해시맵과 배열을 사용하여 효율적으로 구현할 수 있습니다.

4. **잘된 점**: 코드는 비교적 간결하고, 각 단계의 목적이 명확합니다. 또한, 변수 이름이 적절하여 코드의 이해를 돕습니다. 예를 들어, `arr` 변수는 문자의 빈도수를 저장하는 배열을 나타내고, `map` 변수는 각 단어의 문자 빈도수를 저장하는 해시맵을 나타냅니다.

5. **개선 사항**: 코드에서 `Arrays.fill(arr, 101)`을 사용하여 배열을 초기화합니다. 그러나 이 값은 임의로 선택된 것처럼 보입니다. 대신, `Integer.MAX_VALUE`를 사용하여 배열을 초기화할 수 있습니다. 또한, `System.out.print`문을 사용하여 디버깅 정보를 출력합니다. 그러나 이 문들은 실제 코드에서 제거되어야 합니다. 마지막으로, 변수 이름을 더 명확하게 할 수 있습니다. 예를 들어, `arr` 변수를 `charFrequency`로 이름을 변경할 수 있습니다. 이러한 변경으로 코드의 가독성과 유지보수성이 향상될 수 있습니다.
