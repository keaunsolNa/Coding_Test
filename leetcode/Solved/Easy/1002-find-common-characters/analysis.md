# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오전 9:21:06 |
| Language | java |
| Runtime | 2 ms (Beats 0.0%) |
| Memory | 45.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-common-characters/submissions/)

## Code Review

**코드 리뷰**

1. **시간 복잡도**: O(n \* m) - 여기서 n은 단어의 개수, m은 단어의 평균 길이입니다. 각 단어를 한 번씩 반복하여 문자의 빈도수를 계산하고, 이를 통해 공통 문자를 찾습니다.
2. **공간 복잡도**: O(n \* m) - 각 단어의 문자 빈도수를 저장하기 위해 HashMap을 사용하며, 최종 결과를 저장하기 위해 ArrayList를 사용합니다. 따라서 공간 복잡도는 입력 크기에 비례합니다.
3. **풀이 접근법**: 이 코드는 해시 테이블을 사용하여 각 단어의 문자 빈도수를 계산하고, 이를 통해 공통 문자를 찾습니다. 각 단어의 문자 빈도수를 비교하여 공통 문자의 최소 빈도수를 찾습니다.
4. **잘된 점**: 코드는 비교적 간결하며, 해시 테이블을 사용하여 효율적으로 문자 빈도수를 계산합니다. 또한, 공통 문자를 찾는 로직이 명확하게 구현되어 있습니다.
5. **개선 사항**: 코드에서 `Arrays.fill(arr, 101)`을 사용하여 초기화하는 부분은 불필요합니다. 대신, `int[] arr = new int[26]`를 사용하여 초기화하면 0으로 초기화됩니다. 또한, `System.out.print`문을 사용하여 디버깅하는 부분은 제거하는 것이 좋습니다. 또한, 변수명이 좀 더 명확하게 지어졌으면 좋았을 것입니다. 예를 들어, `arr` 대신 `charFrequency`로 이름을 지으면 코드의 의도를 더 명확하게 나타낼 수 있습니다.
