# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오전 9:22:36 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 42.9 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-common-characters/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n \* m)입니다. 여기서 n은 단어의 개수, m은 단어의 평균 길이입니다. 각 단어에 대해 문자 빈도수를 계산하고, 이를 전체 단어에 대해 반복하기 때문입니다.

2. **공간 복잡도**: O(n \* m)입니다. 각 단어에 대해 문자 빈도수를 저장하는 해시맵을 사용하고, 최종 결과를 저장하는 리스트를 사용하기 때문입니다.

3. **풀이 접근법**: 이 문제는 해시맵을 사용하여 각 단어의 문자 빈도수를 계산하고, 이를 비교하여 공통 문자를 찾는 접근법을 사용했습니다. 각 단어의 문자 빈도수를 계산하고, 이를 전체 단어에 대해 반복하여 공통 문자를 찾습니다.

4. **잘된 점**: 코드는 비교적 간결하고, 해시맵을 사용하여 문자 빈도수를 계산하는 부분은 잘 구현되어 있습니다. 또한, 공통 문자를 찾는 로직도 명확하게 구현되어 있습니다.

5. **개선 사항**: 코드에서 일부 불필요한 부분이 있습니다. 예를 들어, `map.putIfAbsent((char)('a' + i), 0);` 부분은 실제로 필요한 부분은 아닙니다. 또한, `System.out.print(map);`이나 `System.out.println(target);`과 같은 디버깅용 코드는 제거하는 것이 좋습니다. 또한, 변수 이름을 더 명확하게 지어주는 것이 좋습니다. 예를 들어, `arr`를 `commonCharCount`로 이름을 지어주는 것이 더 좋습니다.
