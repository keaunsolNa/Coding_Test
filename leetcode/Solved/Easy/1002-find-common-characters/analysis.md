# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오전 9:16:01 |
| Language | java |
| Runtime | 1 ms (Beats 0.0%) |
| Memory | 45.5 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-common-characters/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(N * M)입니다. 여기서 N은 단어의 개수, M은 단어의 평균 길이입니다. 각 단어에 대해 문자열을 배열로 변환하고, 해시맵을 사용하여 문자의 빈도를 계산하며, 배열을 업데이트하는 작업이 수행되기 때문입니다.

2. **공간 복잡도**: O(N * M)입니다. 각 단어에 대해 해시맵을 사용하여 문자의 빈도를 계산하기 때문에, 최악의 경우 모든 단어가 서로 다른 문자로 구성된 경우 해시맵의 크기가 N * M이 될 수 있습니다. 또한, 결과를 저장하기 위한 리스트도 필요합니다.

3. **풀이 접근법**: 이 문제는 해시맵과 배열을 사용하여 문자의 빈도를 계산하는 접근법을 사용했습니다. 각 단어에 대해 문자의 빈도를 계산하고, 모든 단어에서 공통으로 나타나는 문자의 최소 빈도를 찾습니다. 이를 위해 배열을 사용하여 각 문자의 최소 빈도를 저장하고, 리스트를 사용하여 결과를 저장합니다.

4. **잘된 점**: 코드는 문제를 명확하게 이해하고, 필요한 변수와 자료구조를 적절하게 사용했습니다. 또한, 코드는 비교적 간결하고, 쉽게 이해할 수 있습니다.

5. **개선 사항**: 코드에서 `Arrays.fill(arr, 101)`을 사용하여 배열을 초기화했습니다. 하지만, 이 값은 임의로 선택된 값으로, 코드의 의미를 명확하게 이해하기 어렵습니다. 대신, `Integer.MAX_VALUE`를 사용하여 배열을 초기화하면, 코드의 의미를 더 명확하게 할 수 있습니다. 또한, 변수명이 조금 더 명확하게 사용되었으면 좋았을 것입니다. 예를 들어, `arr` 대신 `minCount`를 사용하여, 배열이 각 문자의 최소 빈도를 저장한다는 것을 명확하게 나타낼 수 있습니다.
