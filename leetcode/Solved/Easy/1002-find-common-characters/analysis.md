# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오전 8:59:10 |
| Language | java |
| Runtime | 0 ms (Beats 0.0%) |
| Memory | 45 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-common-characters/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(N \* M)입니다. 여기서 N은 단어의 개수, M은 단어의 평균 길이입니다. 각 단어를 한 번씩 순회하며 문자의 빈도를 계산하고, 이를 통해 공통 문자를 찾기 때문입니다.

2. **공간 복잡도**: O(N \* M)입니다. 각 단어의 문자 빈도를 저장하기 위해 해시맵을 사용하며, 최종적으로 공통 문자를 저장하기 위해 리스트를 사용하기 때문입니다.

3. **풀이 접근법**: 이 문제는 해시맵과 문자 빈도 계산을 사용하여 공통 문자를 찾습니다. 각 단어의 문자 빈도를 계산하고, 이를 통해 공통 문자를 찾는 접근법을 사용합니다. 이는 문자열 처리와 관련된 문제에서 자주 사용되는 패턴입니다.

4. **잘된 점**: 코드는 읽기 쉽고, 변수명이 명확합니다. 또한, 문제를 해결하는 데 필요한 로직을 명확하게 구현했습니다. 특히, 문자 빈도를 계산하고, 이를 통해 공통 문자를 찾는 부분이 잘 구현되어 있습니다.

5. **개선 사항**: 코드는 이미 잘 구현되어 있지만, 몇 가지 부분을 개선할 수 있습니다. 예를 들어, 문자 빈도를 계산할 때, `Arrays.fill(arr, 101)` 대신 `int[] arr = new int[26]`를 사용하고, 이후에 각 문자의 빈도를 계산할 때 0으로 초기화할 수 있습니다. 또한, `String target = String.valueOf(arr[i] + 'a')` 대신 `char target = (char) (arr[i] + 'a')`를 사용하여 문자를 직접 생성할 수 있습니다. 이러한 개선은 코드의 효율성을 높이고, 더 읽기 쉽게 만들 수 있습니다.
