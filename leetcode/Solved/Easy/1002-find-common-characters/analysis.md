# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 8. 5. 오전 9:19:50 |
| Language | java |
| Runtime | 2 ms (Beats 0.0%) |
| Memory | 45.3 MB (Beats 0.0%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/find-common-characters/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(n \* m) - 여기서 n은 단어의 개수, m은 단어의 평균 길이입니다. 각 단어를 한 번씩 순회하고, 각 단어의 문자를 다시 한 번씩 순회하기 때문입니다.

2. **공간 복잡도**: O(n \* m) - 각 단어의 문자를 HashMap에 저장하고, 결과를 ArrayList에 저장하기 때문입니다. 최악의 경우, 모든 단어가 같은 문자로 구성되어 있을 수 있습니다.

3. **풀이 접근법**: 이 풀이는 해시 맵을 사용하여 각 단어의 문자 빈도를 계산하고, 모든 단어에서 공통으로 나타나는 문자의 최소 빈도를 찾는 접근법을 사용합니다. 이 접근법은 문제를 효율적으로 해결할 수 있습니다.

4. **잘된 점**: 코드는 비교적 간결하고, 문제를 명확하게 이해하고 있습니다. 또한, 각 단어의 문자 빈도를 계산하고, 공통 문자를 찾는 로직이 잘 구현되어 있습니다.

5. **개선 사항**: 코드에서 `Arrays.fill(arr, 101)`은 불필요한 초기화입니다. 대신, `int[] arr = new int[26]`으로 초기화하고, 각 단어의 문자 빈도를 계산할 때 최소 빈도를 찾을 수 있습니다. 또한, `System.out.print`문과 `System.out.println`문은 디버깅 용도로 사용되지만, 실제 코드에서는 제거하는 것이 좋습니다. 최적화 가능한 부분은 없지만, 코드를 더 간결하게 만들 수 있습니다. 예를 들어, `Map<Character, Integer> map`을 사용하는 대신, `int[]`를 사용하여 문자 빈도를 계산할 수 있습니다.
