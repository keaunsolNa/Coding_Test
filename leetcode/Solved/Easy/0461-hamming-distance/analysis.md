# Analysis

| Item | Value |
|------|-------|
| Submitted | 2026. 7. 22. 오후 3:01:45 |
| Language | java |
| Runtime | 2 ms (Beats 3.6%) |
| Memory | 42.6 MB (Beats 8.6%) |

## Submission

[View on LeetCode](https://leetcode.com/problems/hamming-distance/submissions/)

## Code Review

코드 리뷰입니다.

1. **시간 복잡도**: O(log(max(x, y)))입니다. 두 수를 이진수로 변환하고, 길이를 맞추는 과정은 상수 시간에 수행되며, 이후 반복문을 통해 각 자리수를 비교하는 과정은 두 수 중 최대값의 이진수 자리수에 비례합니다.

2. **공간 복잡도**: O(log(max(x, y)))입니다. 두 수를 이진수로 변환하여 문자열로 저장하고, 이를 배열로 변환하는 과정에서 사용하는 공간은 두 수 중 최대값의 이진수 자리수에 비례합니다.

3. **풀이 접근법**: 이진수 변환과 문자열 비교를 사용한 알고리즘입니다. 두 수를 이진수로 변환하고, 길이를 맞춘 후 각 자리수를 비교하여 다른 자리수의 개수를 세는 방식입니다.

4. **잘된 점**: 코드는 비교적 간단하고, 이해하기 쉽게 작성되어 있습니다. 또한, 두 수의 이진수 길이를 맞추는 로직이 정상적으로 작동합니다.

5. **개선 사항**: 현재 코드는 이진수 변환과 문자열 비교를 사용하고 있습니다. 하지만, 비트 연산을 사용하여 더 효율적으로 해결할 수 있습니다. 예를 들어, XOR 연산을 사용하여 두 수의 차이를 구한 후, 결과의 1의 개수를 세는 방식입니다. 이는 더 간결하고, 효율적인 해결책입니다. 예를 들어, `Integer.bitCount(x ^ y)`를 사용할 수 있습니다.
